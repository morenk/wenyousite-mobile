# 温油站 Flutter 移动端交付计划

本文件记录阶段边界；实时模块行为与验收以 [`docs/modules/`](docs/modules/) 为准。

## 产品基线

- Android 8+、手机竖屏、游客直达首页。
- 底部导航：首页、搜索、通知、我的；悬浮按钮创建主题。
- 锁定 Foundation 已发布版本：纯白单列画布、柔粉克制强调、深色正文与三角色自托管字体；先完成功能闭环，再独立视觉精修。
- V1 包含认证、阅读、搜索、创作管理、关系、账号、图片、提及、骰子、草稿和站内通知。
- V1 不含私聊、收藏表情、FCM、举报/后台、离线浏览/发帖、暗色主题、阅读进度和子贴标签。

## 工程不变量

- `contracts/openapi.json` 是 API 机器事实源，Markdown v2 黄金语料是正文跨端事实源。
- Flutter Quill Delta 仅存在于编辑器内存；自研 Codec 与 Markdown v2 双向转换，所有快照仍是完整 Markdown。
- 每请求 UUID v4 `X-Request-ID`；登录/完成注册携带 mobile 头；并发 `40101` 只刷新一次。
- 创建主题、子贴、楼层和回复使用稳定 `clientRequestId`，超时重试不换键。
- Cursor 不解码、不计算；`40007` 清空并从第一页加载。
- 代码、测试、模块当前事实文档同切片更新。

## 里程碑

1. `0.1.0-dev`：公开仓库、CI、文档、主题、固定契约生成、网络/会话/持久化基线。
2. `0.2.0-dev`：应用壳、游客模式、认证、恢复、鉴权回跳与终端会话。
3. `0.3.0-dev`：首页、搜索、主题、子贴、楼层和用户主页。
4. `0.4.0-dev`：回复、点赞、收藏、关注、订阅、拉黑和通知。
5. `0.5.0-dev`：Flutter Quill 编辑器、Markdown ↔ Delta Codec、图片、提及、骰子、本地/云草稿。
6. `0.6.0-dev`：主题工作台、子贴管理、成员、玩家和私密邀请。
7. `0.7.0-dev`：完整账号生命周期、性能、弱网、无障碍、模拟器与真机验收。
8. 后续独立里程碑：视觉精修。

## 单切片完成定义

- 主路径与加载、空、错、重试、权限/冲突状态可操作。
- 关键状态在刷新和应用恢复后正确。
- 格式、fatal analyze、全量测试、API 生成一致性、Markdown 黄金语料和 docs:check 全绿。
- 依赖、Android、插件或里程碑变化额外构建 Debug APK。
- 无敏感信息、调试残留或伪实现；文档同步或提交中说明 `Docs-Impact: none - 原因`。
- 原子提交并推送 `dev`，CI 绿色后再开始下一切片。

`dev → main`、去除 dev 后缀和正式 Tag 只由维护者明确决定。
