# 动画播放语料

`hello_loop_2.webp` 来自 Flutter 3.44.8 SDK 的 `engine/src/flutter/lib/ui/fixtures/hello_loop_2.webp`，是两轮播放的真实动画 WebP；其循环语义由同目录上游 `painting/image_decoder_unittests.cc` 验证。

仅用于验证本地受控解码、循环与取消，不含用户媒体。授权条款见同目录 `FLUTTER_LICENSE`。
