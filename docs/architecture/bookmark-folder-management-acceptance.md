# 自建收藏夹管理候选验收

状态：候选／待核对负责人真机安装包；未正式发布。

## 范围和前置条件

- 主题帖与动态两套目录分别管理。默认收藏夹不可重命名或删除；选中自建夹时才显示顶栏管理入口，仍保留最右侧“新建”。
- 重命名预填原名称、自动选中，按现有 1–24 字规则校验；失败保留输入和弹窗。删除前说明收藏会移至默认夹、内容不会删除；成功采用服务端返回的 `destinationFolderId` 切换，失败保留原夹及其内容。
- 所需 Backend PR #20 已部署：公网 `/api/v1/meta` 为 `e214fd18637cb10d79576c5ab5a4cf42340fef71` / `5.23.0-dev.20260913.1`。本候选未使用负责人账号执行写入联调。

## 自动验证与候选包

- 在最新 `origin/dev` 上整合后，`npm run check:apk` 通过：固定契约与生产 API 校验、生成客户端一致性、格式、应用及生成包静态分析、架构与模块文档、158/158 已接入 API 覆盖、全量 Flutter 测试、18 项 Windows 发布工具测试和 Android Debug 构建。
- 收藏夹专项测试覆盖 360/400dp 顶栏、默认夹无管理入口、两类自建夹菜单、重命名输入与失败保留、删除提示、服务端返回目标夹切换及失败恢复；控制器测试覆盖 trim、并发管理请求和释放后的迟到响应。
- 构建产物：`C:/Users/quhui/.codex/worktrees/3cfc/wenyousite-mobile/build/app/outputs/flutter-apk/app-debug.apk`，223,527,606 字节，SHA-256 `6226447D8081A4A13E428520CDC30DF6E94BE65E73A1A912EBE153B5AC2D75E8`。
- APK 标识：`site.wenyou.app.debug`，“温油站 Debug”，`versionName=0.7.1-debug`、`versionCode=95`、最低 API 26。此 Debug 包不等于正式发布包；没有通过本任务安装到真机或上传发布桶。

## 负责人复验路径与结论

1. 核对设备安装的是上述包名和 SHA-256 对应的本次整合候选，而非同为 build 95 的旧版 Debug 或正式包。
2. 分别在主题帖和动态目录执行：新建自建夹 → 重命名 → 将一条专用测试内容收藏到该夹 → 删除 → 确认内容和数量进入对应默认夹。
3. 检查默认夹无管理入口、长名称、360/400dp、小屏大字体、键盘提交、删除确认、失败后输入与列表保留、读屏标签。

负责人已表示 Mobile 真机验收过，但尚未提供安装包摘要或对应提交，无法确认是否为本次整合候选。核对前保持候选状态；自动测试和构建不代替负责人对同一候选的真机结论。
