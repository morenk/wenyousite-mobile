# 编辑器 v7 Windows 验收

## 范围与基线

- 后端部署与契约 revision：`6b0c9c83ecd4532490cf07ad38eff9e10eb277b3`，Web 交接提交 `c864e7f`；后端迁移审查的旧移动端为 `9d6a7c4e29cc20c080ffa47ac18aa2736c3d518e`。
- HTTP API `5.18.0-dev.20260905.1`，公网 Markdown v5；round-trip fixture v7 与图片对齐 fixture revision 2 是测试版本，正文仍仅保存 Markdown。
- 当前移动开发版本 `0.7.0-dev.1+94`。本切片生成 Windows 本地验收包，不调整线上推荐 93 或最低版本，不合并 main、不打正式 Tag。
- 目标：已有富文本在插入、删除、保存和重开后保持文字及样式，星号不泄露为正文；无法证明无损时保留编辑与草稿并阻止提交。

## 自动回归

- 真实 `LiteralTextQuillController` 消费全部 48 条 `editCases`，明确设置光标 marks，再执行 replaceText；检查首／中／尾插入、逐段全部样式、规范序列化、重开幂等和删除恢复。
- 16 组中间输入逐一覆盖五类 `inlineInsertTexts`，包括空格、Markdown 标点／反斜杠、中文标点、组合 Emoji 与反引号。
- 引用内连续粗体、软换行、显式空段和对齐标题／图片相邻；六类原子节点边界、不同链接目标、真实样式边界和历史等价嵌套。
- 公共出口核对文字、完整 marks、块属性、空段、软换行、对齐和节点身份；会话失败不回调新 Markdown，实际回复页面失败零请求且不关闭编辑器，修复后可再次保存。
- 2026-09-07 完整本地门禁 `npm run check` 通过：2,220 项 Flutter 测试通过，真实 Sentry 收件测试 1 项按配置跳过；16 项 Windows 工具测试通过。格式、应用／生成客户端分析、架构、21 个模块文档、154/154 API 覆盖与契约再生成一致性均通过。自动检查不替代下面的真机旅程。

## 本地安装记录

- Windows 本地 `flutter build apk --debug` 与 `flutter build apk --release --target-platform android-arm64` 均成功；制品分别为 `build/app/outputs/flutter-apk/app-debug.apk` 与 `app-release.apk`。
- 2026-09-07 已经 ADB 覆盖安装 ARM64 签名包到已连接真机，包名 `site.wenyou.app`，版本 `0.7.0-dev.1+94`，保留应用数据。
- 安装包与手机内 APK 的 SHA-256 一致：`361525ed6459aa22b452b79c6d5e1fcf0877b9d5ecc5b7d0ad907ad73a3dc508`。
- APK 签名、16 KB ZIP／ELF LOAD 段对齐、仅 ARM64 原生库与三套现有字体检查通过。安装成功不代表登录、输入或发布旅程已验收。

## 项目负责人真机清单（安装已完成，旅程待执行）

仅使用专用测试账号和新建的可识别测试内容。不得修改用户原帖，清理仅限本轮明确创建的测试内容，不注销账号、不批量删除共享数据。

账号准备：使用能接收验证码的独立邮箱，从 Web 或 App 正常注册普通账号，例如用户名「温油测试员」，随后在两端登录同一账号。本轮编辑器旅程一个账号即可，不需要管理员权限、数据库标记或环境变量。密码由负责人保管，不写入聊天、仓库或日志；测试内容标题带「测试」与日期，便于确认清理范围。

1. 打开已安装的本轮 ARM64 签名包；确认版本与构建号，现有登录、草稿和字体正常。使用已有 Debug 会话时重启会话，进入目标编辑页。
2. Web 新建测试正文，包含粗体、斜体、删除线、链接的 15 种组合和行内代码；Android 分别在首／中／尾输入，再输入空格、`*[_]~\`、中文标点、组合 Emoji 和反引号，删除新增字并重新输入。确认文字及样式即时正确，没有额外星号或斜杠。
3. Android 在引用内连续粗体、软换行处编辑，在对齐的 H2/H3 和独立图片前后插入一至两个空段；保存后 Android 重开，再由 Web 重开，确认逐段样式、空段数、标题／图片对齐与原子节点身份不变。
4. 反向执行：Android 新建上述测试正文 → Web 修改格式／插空段 → Android 重开编辑 → 保存 → Web 重开。覆盖云草稿保存／恢复、楼层或回复发布、帖子编辑更新。
5. 断网或保存失败时，确认正文留在编辑器，重新进入后可恢复草稿；操作成功只出现一条测试帖子。两端确认结果后清理本轮测试内容。

本次交接没有专用测试账号，双向登录发布旅程和真机视觉／输入验收仍待负责人完成。通过后才能另行决定推荐 build 94；若 94 已发布则使用下一个未发布构建号，最低支持版本不提高。

相关说明：[编辑器模块](../modules/editor.md)、[Codec 架构](editor-codec.md)、[后端迁移说明](../../contracts/mobile-client-guide.md#编辑器-v7-windows-迁移)。
