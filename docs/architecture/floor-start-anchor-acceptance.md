# 楼层开头定位验收

状态：负责人验收通过／修复完成；保留首轮失败记录。

## 验收结论

2026-09-11，负责人在二轮候选安装后明确回复“验证通过，合并清理分支”。对应源码 `372698ac591878de5557b95232098f29ff2ef352`，Debug APK SHA-256 为 `8931A5C72D90317083773C6BAE09CBD1E67C3B12E346703C08974630BCE4610A`；楼层开头定位及 AT 回复跳转问题验收通过。随后将最新 `dev` 的阅读快翻与页面拆分合入任务分支，保留快翻主动导航释放定位的行为，列表锚点迁移到拆分后的内容组件。

验收制品、原始本地复现语料及检查日志归档至 `D:/code/wenyousite/artifacts/mobile-floor-start-anchor-20260911`，用于在清理临时 Worktree 后继续溯源。下文候选、待验收及本地路径描述保留各轮当时的记录。

合并兼容回归：178 项定位、主题详情、楼中楼、动态评论及阅读快翻测试通过，包含两份真实语料、快翻后释放自动定位、四种宽度的快翻页面检查；应用静态分析零问题，架构和模块文档检查通过。保留最新 `dev` 的契约来源和 Foundation v6.10.0，不退回旧版本。合并回归日志同目录归档。

## 负责人复验反馈

- 候选 `b451ab0` 已按负责人指令通过 `adb install -r` 安装，返回 `Success`；负责人明确要求跳过设备版本核验。
- 负责人反馈 AT 回复跳转失灵：进入楼中楼后闪动数次，最终停在最底层，未保持指定回复。
- 失败坐标：<https://wenyou.site/threads/cmtrvxfsy004s7q8a240qqb29/posts/cmtwi05qf00i57qvy2gl4hd1j/replies?post=cmtwkqods000m7q1it9sf3vm1>。
- 本反馈属于原定位问题的验收失败，保留此前检查、构建与安装证据。

## 二轮根因与候选

公开只读接口返回 18 条回复，目标为第 9 条、正文 71 字符；第 5、7、8 条分别为 1,067、1,058、1,043 字符的长正文。保持原始正文和顺序、替换测试 ID 与作者的真实回复页回归，在 360×800 和 400×800 下均复现目标最终不在布局中的失败。

旧定位在目标未渲染时使用“目标序号占比 × 估算总滚动高度”，最多尝试 6 次。实测 360 宽窗口先跳到约 5,581，再跳到 16,349，随后在 6,558 与 16,349 之间反复跳动；可变高度懒列表不断重估总高度，最终滚动回弹到底部。所有回复已在同一页，证明本例无需分页即可触发。

二轮候选沿真实已布局行的末端向前查找，反向跳转则从已布局首行向前一屏查找；进入目标所在行后再按块开头对齐。索引和几何未推进时停止重复重排，内容变化可重新尝试；不按固定次数放弃远距离目标。保活行虽然保留上下文及旧布局偏移，实际不参与绘制，候选明确排除此类几何。

- 实际回复正文只留在本地 `build/floor-start-validation/replies-repro.local.json`，SHA-256 为 `3FCA9B5FBA0E468C1A8DC26D17001109BAE95752E1608C3248BEADDF712235BD`。页面测试用 `REPLIES_REPRO_FILE` 参数读取；仓库默认回归使用构造正文，不提交作者内容。
- 真实语料两种宽度旧实现均失败、候选均通过；默认构造语料两种宽度旧实现也均失败、候选通过。旧失败日志分别为 `replies-real-old.log` 与 `replies-synthetic-old.log`。
- 已补查跨多屏长短混排、超长前置正文、返回保活楼层、拖动中止定位，以及首轮长短／末尾／吸顶／布局变化回归。
- 二轮 `npm run check` 完整门禁通过：2,482 项 Flutter 测试、17 项 Windows 工具测试通过，1 项既有外部回执测试未启用；应用与生成客户端静态分析、格式、架构、模块文档、API 覆盖、生成一致性及公网契约核对通过。日志为 `replies-quality.log`。
- 用两份真实本地语料另跑原长楼层两个入口和本次 AT 回复两种宽度，共 4 项页面回归通过，日志为 `replies-real-final.log`。二轮制品记录见下方；当前自动检查不等于负责人真机通过。

二轮候选 APK：`build/app/outputs/flutter-apk/app-debug.apk`，应用“温油站 Debug”，包名 `site.wenyou.app.debug`，开发版本 `0.7.0-dev.1-debug (94)`，203,745,402 字节。SHA-256：`8931A5C72D90317083773C6BAE09CBD1E67C3B12E346703C08974630BCE4610A`。`flutter build apk --debug --no-pub` 成功，日志为 `replies-build.log`；2026-09-11 按既有授权直接执行 `adb install -r`，返回 `Success`，依负责人指令跳过设备版本核验，未代替负责人操作页面。

## 原始场景

- 负责人提供的目标：<https://wenyou.site/threads/cmtjrn0xc05rp7qvw9t7zs1iv?post=cmtvt31qc00497qc2xwuu6ama>。
- 在帖子中点击“定位最新回复”（移动端按钮为“跳到最新发言”），实际停在目标内容靠中间的位置；预期作者头像、楼层号和正文开头可见。
- 另一个现象：从通知进入目标后，作者头像在屏幕上方，需要回滚才能看到。该通知未提供具体 ID，暂以同一坐标入口和楼中楼页面验证。
- 2026-09-11 公开只读接口核对目标为主楼第 7 层，无父楼层；原始 Markdown 为 6,713 个字符、263 行、含一张图片。原文仅保留在本地，不提交作者正文或图片地址。
- 本地原文 UTF-8 SHA-256：`C737E914F1B749034C3C73F6E910037BD37C73D1E41413791DE35965D121C801`。

## 根因与定位规则

共享定位使用 `Scrollable.ensureVisible(alignment: 0.12)`。此比例基于阅读区与目标高度之差；当目标高于阅读区时会越过目标开头。构造几何回归中，600 高阅读区与 1,800 高目标使开头位于 -144。

原始正文页面回归也已证实同一现象：360×640 视口中，通过最新发言按钮及直接坐标进入，旧实现均将目标顶部定位到约 -1210.38，正确阅读起点为 96（顶栏及吸顶选择器下方）。页面使用真实 Markdown、测试作者与主题上下文，图片由测试环境处理，未在负责人设备上复现。

候选使用开头对齐。Flutter 视口计算负责扣除前方吸顶 Sliver 占位，滚动位置限制在实际范围内：

- 长楼层从目标块顶部开始，作者信息不被吸顶选择器遮挡。
- 短楼层在后续内容足够时也对齐开头；末尾空间不足时停在自然列表底部，整层可见且不新增空白。
- 不足一屏的整页保持自然排版。
- 分页、图片撑高和视口尺寸变化后，在用户未主动拖动时重对齐；用户拖动后保持原有停止自动定位规则。
- 主楼、主题楼中楼、动态评论及回复共用规则；传送门、通知、搜索和最新发言入口无需分别修正。

## 验证与待验收

- 已通过 66 项相关回归，覆盖共享定位、实际正文的直接坐标／最新发言、独立楼中楼页面及动态评论页面。
- 旧算法在几何回归中 9 项失败，实际正文两个页面入口均失败；候选对应检查全部通过。旧失败日志与候选日志保留于本地工作区。
- `npm run check` 完整门禁通过：2,477 项 Flutter 测试、17 项 Windows 工具测试通过，1 项既有外部回执测试未启用；格式、应用与生成客户端分析、架构、文档、API 覆盖、契约校验／再生成及公网兼容核验均通过。
- API 覆盖为 154/154，无缺口。前置同步单独提交，包含生成类型及契约审查清单版本更新；封面播放逻辑不在本候选范围。
- `flutter build apk --debug --no-pub` 成功，APK v2 签名验证通过；第一次构建因本机并行任务内存紧张而主动取消，不计作构建通过。

首轮候选 APK：`build/app/outputs/flutter-apk/app-debug.apk`，应用名称“温油站 Debug”，包名 `site.wenyou.app.debug`，`versionName=0.7.0-dev.1-debug`、`versionCode=94`，203,745,402 字节。SHA-256：`181FC6DF9784D32A51CD04A6761B15E62095CF63A843EF12D965EFD398CB025D`。后续已按负责人要求 ADB 安装成功，未代替负责人操作页面。

页面回归提供 `FLOOR_REPRO_FILE` 编译参数，可用本地原始正文复核通知／传送门坐标入口及最新发言按钮。原文现存于本地 `build/floor-start-validation/target-original-content.local.txt`，默认回归使用构造长正文；实际图片解码和真机渲染仍需手测。

负责人在候选 Debug 应用 `site.wenyou.app.debug` 中执行：

1. 打开原主题，使用“跳到最新发言”，确认第 7 层作者头像、楼层号和正文开头完整可见；若最新目标已改变，另用上面的固定地址复验第 7 层。
2. 从发生过问题的通知进入，分别验证主楼与楼中楼，作者信息均完整可见。
   重点复验本次 AT 回复地址：应停在指定短回复的作者与正文开头，等待两秒后仍保持目标，不能闪动后落到最底层；再主动上下滚动，页面不自动拉回。
3. 通过传送门／搜索进入中间短楼层、末尾短楼层和末尾长楼层，确认没有大块人为尾部空白，也不落在长正文中间。
4. 图片加载完后仍保留目标开头；主动滚动阅读后，图片／分页变化不把页面拉回。

本任务按负责人授权安装候选，不代替负责人操作页面；自动测试、安装和构建不替代上述验收。
