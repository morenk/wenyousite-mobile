# 通知授权引导与默认提示音候选验收

状态：负责人验收通过（2026-09-13）。负责人在最终候选 `bac2746e` 上确认通过，并授权 [PR #32](https://github.com/morenk/wenyousite-mobile/pull/32) 合并清理；不包含正式发布。以下保留候选阶段记录，最终确认见[点击已读与文案验收](notification-read-acceptance.md)。

## 原始反馈与边界

2026-09-13：负责人在上一候选 `a1af3d3d` 上确认通知栏可收到私聊/回复，但没有横幅；手动开启小米“新消息提醒”的悬浮通知后已有横幅。截图显示“后台消息提醒”悬浮关闭、声音无、振动关闭；“新消息提醒”悬浮开启，但声音无、振动关闭、锁屏完全不显示。本次补足权限引导及默认系统提示音，不据此宣称此前后台运行问题全部完成验收。

系统普通通知权限可以申请；小米悬浮通知等频道展示选择需要用户去设置。新频道默认请求系统提示音，但旧频道设为“无”后必须保留，不能通过删频道、换 ID 或私有 API 强制改成有声。不申请悬浮窗、全屏通知、电池豁免等额外权限。

## 基线与实现

- 2026-09-13 重新 fetch Mobile，origin/dev 仍为 `e5eea0de`；本次继续已有未合并候选分支，不触碰主 checkout 中另一任务的候选构建脚本改动。
- 后端远端 dev 与公网 `/meta` 均为 `6fdfa00eaf1f3056ba30f2ffbc529d12eed1c823`，契约 `5.22.0-dev.20260912.2`，与已记录来源一致，无契约升级。Foundation 只读 Git 缓存 fetch origin --tags 后正式最新 `v7.0.0`，与 pubspec 固定版本一致；不改后端、Foundation 或生成客户端。
- 应用壳在登录、提醒开启、前台且平台支持时显示一次非阻断引导；用户可继续原页面或跳过。先保存设备级处理标记，再展示；拒绝、跳过、重启和切号不重复自动询问。读写失败跳过自动引导，设置入口仍可手动操作。
- “允许通知”才触发 POST_NOTIFICATIONS 请求；拒绝后提示前往设置，不自动再请求。授权成功后引导横幅/默认声音，再点“去设置”才进入消息频道。系统设置失败保留上下文重试；账号设置保留手动申请入口。
- 连续点击合并为单次申请；恢复事件等待申请完成。账号、开关变化后旧引导/异步结果不继续导航；正常后台轮询、去重和常驻服务策略不变。
- 消息频道显式开启 playSound，不指定自带音频；本地锁定插件代码确认空 sound 使用系统默认通知 URI。常驻静音，消息尊重旧频道选择和系统音量/静音/勿扰。

## 自动验证

针对性回归共 22 项通过；之后补充原页面状态保留断言，并单独复跑引导组件 5 项全部通过。完整门禁会对最终源码再次验证。实际测试文件：

- `test/core/application/notification_guidance_test.dart`
- `test/core/platform/android_background_notification_gateway_test.dart`
- `test/features/app_shell/background_online_reminders_test.dart`
- `test/features/app_shell/notification_permission_guidance_test.dart`
- `test/features/app_shell/background_reminder_lifecycle_test.dart`
- `test/features/users/background_reminder_settings_panel_test.dart`

覆盖设备记录、并发/读写失败、首次提示、拒绝、跳过、切号、重建、后台等待、设置失败、360dp 双倍字号、原生申请连续点击/恢复竞态、手动重试、稳定频道及默认声音参数。实际横幅和音效必须真机验证；参数断言不能证明厂商最终播放行为。

自查发现可见/隐藏引导若切换页面包裹结构可能重建原页面，已改为固定容器结构，并断言操作后的页面状态在引导收起后保留。此调整在完整门禁的格式、分析、测试和 APK 阶段之前完成；生成客户端与依赖锁文件无差异。

曾按持久化与权限编排启动完整 `npm run check:apk -- -TestConcurrency 2`。负责人随后明确要求此类细节候选先真机验证、不要每轮等待全量测试，已按新要求停止本任务剩余全量检查，保留日志，不影响其他任务的进程。停止时 1524 项通过、1 项既有 Sentry 联网测试跳过，不将未完成的全量测试记为通过。

本轮已完成：公网/固定契约验证、生成一致性、全仓格式、应用与生成客户端静态分析、架构、21 个模块文档和 API 覆盖；候选以针对性回归及独立 `flutter build apk --debug --no-pub` 交付。完整门禁留在负责人验收通过后的合并前执行，不引用上一 APK 的全量结果代替本轮。构建日志为 `build/notification-guidance-build.log`；部分完整检查日志为 `build/notification-guidance-check.log`。

## 候选安装身份与已执行检查

- 源码提交：`95253d88795010aead7be189b38dc5393a81c709`。后续本记录提交仅补充验证证据，不修改构建源码。
- 最终上述六个文件的针对性回归共 22 项通过；日志 `build/notification-guidance-targeted.log`。应用/生成客户端静态分析零问题，格式、架构、模块文档、API 覆盖及契约/生成一致性已通过。完整测试按负责人要求中止，非全量通过。
- 独立 Debug 构建成功，耗时约 79.5 秒，apksigner 校验通过。版本 `0.7.0-dev.1+94`，APK `versionName=0.7.0-dev.1-debug`，包名 `site.wenyou.app.debug`，应用名“温油站 Debug”。
- APK：`D:\code\wenyousite\.worktrees\mobile-background-reminders\build\app\outputs\flutter-apk\app-debug.apk`。
- SHA-256：`39d021dc40c1bd081860684411f99447092a04e22669e825917b8acabaf03105`。
- 安装前仍是前一候选 Debug，更新时间 `2026-09-13 03:38:35`。按负责人既有授权执行 `adb -s 4b9c39b5 install -r` 返回 Success；安装后时间为 `2026-09-13 04:16:33`（Asia/Shanghai），设备内 `base.apk` SHA-256 与候选完全一致。未卸载、清数据、修改通知设置或覆盖正式包。
- 负责人应打开“温油站 Debug”复验以下步骤；安装、签名和自动检查不替代真机授权/声音验收。合并前仍需按负责人要求补齐最终源码完整门禁。

## 负责人复验步骤

1. 覆盖安装后打开“温油站 Debug”，已允许通知的设备只显示横幅/系统默认声音引导，不弹普通授权框；点“暂不设置”，重启和切号不重复提示。
2. 在未授权的专用测试安装环境，登录后先显示提示，未点击前不弹系统权限；点“允许通知”才申请。拒绝后不循环弹框，可在账号设置明确重试；不为测试清除负责人现有应用数据。
3. 点“去设置”进入“新消息提醒”，不是常驻频道。选择系统默认通知音并允许悬浮，屏幕亮着且应用后台，发新私聊与楼层回复：有横幅及系统提示音，点击目标正确，私聊不显示正文。
4. 旧安装原来声音为“无”时，覆盖后仍为“无”；用户手动改成系统默认后才有声。新安装初始消息频道默认声音应跟随系统；常驻卡片在每次进入后台和 30 秒轮询期间均不响。
5. 静音/勿扰/通知音量零继续服从系统，不强行出声。关闭横幅仍收普通卡片；锁屏按用户设置显示或隐藏。

未验证项：上述真机授权/声音行为和上一候选的 30 分钟及 Doze 等完整稳定性清单；安装成功不代替负责人验收。
