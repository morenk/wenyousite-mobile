# 引用空行 Windows 验收

## 当前结论：验收失败／继续排查

2026-09-07，负责人在候选 `029a011` 覆盖安装后反馈“还是没能成功修复”。新截图中 #7 楼编辑态仍为两个引用背景，中间显示字面 `>`；两段可见文字均为“阿三大苏打”。原问题未解决，先前自动回归与安装成功不能作为修复完成依据。对应 APK 为 `0.7.0-dev.1+94`，SHA-256：`dbb2817ea7ad55e5a11e71a2a53f1d28c0f3622f3d0ea8e7be347f7d3764ad0d`。

原楼层的精确 Markdown 与进入编辑器后的实际转换过程尚未复现，根因未确定。下一步应取得保留空格、换行和转义的实际输入，对照读取、草稿恢复、转换及显示路径，形成与原场景对应的回归后再交付候选。未取得这些证据前，不继续把相似样例的缺陷认定为原问题根因。负责人须在新候选上复验并明确通过，才能更新为修复完成。

## 第二轮候选与检查记录（029a011，2026-09-07）

- 用户提供的阅读 HTML 是单个 `blockquote` 内两段（`hh`、粗体与斜体删除线），外部另有一个 `<br>` 空段。该 HTML 不包含原始 Markdown 分隔字符，不能据此确定原楼层命中了哪个解析分支；匿名 GET 仍返回 `40402`，没有可用的登录浏览器。原楼层源码待取得；安装后的负责人页面复验失败，见当前结论。
- 已独立复现另一处前缀遗漏：`>` 后省略普通空格、使用 Tab 或合法前导空格时，紧随的 Unicode 空白／零宽正文会连同 `>` 降为普通行。修复统一 `MarkdownContent.quoteLineContent`，保留正文字符，不通过 trim 或删除不可见字符伪造等价。
- 15 条新回归在旧代码全部失败，修复后通过；检查单个 Quill 引用块、逐字文本、规范写回、独立 Markdown 阅读结果和重复重开。原有 16 条空引用／保护区回归继续通过。
- 新增两张 360dp 阅读／编辑配对 Golden：按反馈 HTML 构造的双段引用，以及紧随引用标记的零宽正文；均验证连续引用背景、引用外空段、真实输入后保存、完整样式和重开。不能以这两份构造样例替代原楼层验收。
- Markdown 核心及编辑器相关 720 项测试通过；完整门禁 `npm run check` 通过：2,253 项 Flutter 测试、16 项 Windows 工具测试通过，真实收件 1 项按配置跳过；应用与生成客户端分析、架构、21 个模块文档、154/154 API 覆盖及契约再生成一致性通过。两张构造样例图片已本地检查；这些结果未覆盖负责人随后复验仍失败的原场景。
- Debug 与 ARM64 签名 APK 已构建成功；签名、仅 ARM64、三套字体保留和 16 KB ZIP 对齐检查通过。Debug 切换 Release 时首次跳过依赖获取导致插件注册文件残留测试插件，按标准 `flutter build apk --release --target-platform android-arm64` 重新生成后通过，未手改生成文件。
- 2026-09-07 23:47 已 ADB 覆盖安装 `site.wenyou.app`（`0.7.0-dev.1+94`），保留应用数据。安装前目标包校验值与首轮记录一致；安装后手机内 APK 与本轮制品 SHA-256 一致：`dbb2817ea7ad55e5a11e71a2a53f1d28c0f3622f3d0ea8e7be347f7d3764ad0d`。原楼层未保存，安装成功不代表其编辑页面已经验收。
- 公网后端现为 `1f9c7492a8b1e2df47a31c762e049474c8b9944c`；fetch 后对照原记录的 `6b0c9c8`，仅备份监控及 Redis 耐久脚本变化，所有同步契约与指南无差异。严格部署来源校验要求 SHA 一致，已通过独立 `chore` 同步元数据、当前计划和全部模块审查提交；公网契约校验已恢复通过。Foundation 已 fetch tags，最新正式版仍为固定依赖 `v6.9.0`。

以下保留首轮候选的检查与安装记录，两轮候选均未通过原问题验收。

## 首轮范围与基线（56321e3）

- 后端 revision `6b0c9c83ecd4532490cf07ad38eff9e10eb277b3`，HTTP API `5.18.0-dev.20260905.1`、公网 Markdown v5、编辑器 fixture v7；镜像、已记录契约与公网 `/meta` 一致，无契约或依赖变更。
- 最小复现为 `> **甲**`、独占 `>`、`> *乙*` 三行。修复前实际 Quill 文档为两个引用块夹普通 `>` 文本；修复后为含一个空行的单个引用块。正文仍只保存 Markdown，规范写回 `>`。
- 反馈楼层匿名读取返回不可见，当前没有可用的浏览器会话；回归使用不含原帖内容的构造样例，未修改原楼层。原楼层需由有访问权限的负责人复核。

## 自动回归

- 五种空引用空白拼写，检查可见文本、完整行内格式、单个 Quill Block、引用内部两段的 Markdown 阅读结构、保存和重开幂等。
- 首部、尾部与连续引用空行保留数量；真实 `RichEditorSession` 在引用空行输入粗体、保存、删除后恢复原文。
- 独立引用之间的普通空行保留；转义、行内代码、围栏代码、缩进代码和嵌套引用中的可见符号不被吞掉。

2026-09-07 完整门禁与 Debug APK 构建通过：2,236 项 Flutter 测试通过，真实收件测试 1 项按配置跳过，16 项 Windows 工具测试通过；分析、文档、架构、API 覆盖及再生成一致性通过。

ARM64 签名 APK 构建成功，验签、16 KB ZIP／ELF 对齐与三套字体保留检查通过；已 ADB 覆盖安装 `site.wenyou.app`，保留应用数据。手机内 APK 与本次制品 SHA-256 一致：`fce0eacb7aa72272093cb19a2daf4318949cfb6f7de64ad0e32df7a3b2e959fb`。安装完成不代表原楼层视觉或双向发布旅程已验收。

## 构造样例的缺陷与回归边界

首轮构造样例已证实的缺陷在移动端 Markdown → Delta 的实现：原块模型只识别 `> ` 后有正文的引用，空 `>` 仅在阅读复制分支处理，普通编辑因此生成不带引用属性的可见标记行。阅读组件按完整 Markdown 块解析，编辑转换按物理行识别，两条路径产生不同语义。这一缺陷不能等同于反馈楼层的根因；负责人复验已确认候选未解决原问题。Markdown 本身能够表达引用内分段。

共享编辑 fixture v7 的引用基础样例只有单行引用，48 条 editCases 集中覆盖行内样式组合；缺少引用分段编辑的结构预期。仅验证 encode/decode 往返，或将第一次解码的 Delta 作为保存校验基准，会遗漏“解码已错但往返自洽”的情况。本次新增显式 Quill 单块／空行／marks 预期和独立 Markdown 块解析断言。

后续共享契约应在后端授权工作区发布块结构编辑矩阵，再由三端共同消费：覆盖段落／标题／引用／列表／图片的边界、空行与软换行，叠加行内 marks、转义和原子节点，并验证打开编辑、首中尾输入、回车、退格、格式切换、复制粘贴、保存及两端重开。预期需明确文字、marks、块归属、分段和节点身份，不能由待测 Codec 自动生成。该跨端共享矩阵尚未发布，不以本次局部回归通过代替全部样式验收。

## 项目负责人真机清单

1. 关闭旧编辑器并重新打开反馈楼层，确认中间不再显示 `>`，上下引用框连续且原有文字、样式与间隔完整。此项只检查，不保存原楼层。
2. 使用专用普通账号在 Web 新建可识别的引用测试内容，含粗体上段、引用内空行、斜体下段；Android 进入编辑器，分别输入、删除，再保存并两端重开，核对引用和样式。
3. 检查首尾及连续引用空行、两个独立引用块，以及普通文本中的转义 `>` 和代码中的 `>`。只清理本轮创建的测试内容。

版本沿用 `0.7.0-dev.1+94`；线上推荐仍为 93，不提高最低支持版本。原楼层复验已失败，双向发布旅程未验收；须在后续候选上由负责人明确通过后才能记为完成。
