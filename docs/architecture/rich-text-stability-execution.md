# 富文本稳定性专项执行记录

状态：本轮实现与 S5 现场验收已于 2026-09-12 取得负责人明确通过，合并记录与制品归档见 [S5 验收记录](rich-text-s5-acceptance.md)。下文保留各阶段原始验证事实，不把人工通过改写为此前未执行的自动化结果。

## 1. 范围与基线

- 依据：[专项计划 PR #16](https://github.com/morenk/wenyousite-mobile/pull/16)，计划提交 `5ff53fbb28da300a3ce17dd5b575b4a9ca05e1df`。负责人授权继续推进至阶段 4，未授权合并或部署。
- 移动端实现基线：`2fb2242`，独立 Windows Worktree `mobile-rich-text-stability`。
- 后端事实源与共享测试契约在 VPS 独立任务发布，客户端从已提交 SHA 同步。现有 Markdown 持久化、Quill 和 Web 编辑器继续使用。
- 自动验证与 S5 真机、跨端人工旅程分开记录；历史已验收问题不因本专项重开。
- 契约 chore `36b3b1d` 固定 Backend `fae818e8884813d476011dd936caa3463225c1a1`。fixture revision 2 的 SHA-256 为 `c3499979edc72ec1b39905246d541ae561dc02a62e312a792be307f4fd596814`；后端比较器后续修正不改变 fixture/schema。
- 保留 Quill、Web 编辑器、Markdown 存储及现有来源属性；不新增 wire version、第二份权威正文或数据库迁移。Foundation 仍固定 `v6.9.0`，本轮不改视觉规范、Token 或依赖。

## 2. 阶段追踪

| 阶段 | 必需证据 | 当前进展 |
| --- | --- | --- |
| S1 行为契约 | 17 个操作序列、21 项兼容矩阵、结果 Schema | 已固定同步；newline 27 条、v7 48 条不变，预期由独立规则给出 |
| S2 执行与诊断 | 真实 Quill 命令/键盘、剪贴板、页面保存/退出、独立阅读投影 | Flutter 12 个正常操作、4 个帖子保存故障、1 个主题关闭故障；可序列化检查点另用真实阅读组件验证 |
| S3 分族实现 | 字面代码、NBSP 阅读、撤销选区、退出失败与未知原文保护 | 相关回归通过；列表/空格式块沿用独立任务，不重开历史已验收问题 |
| S4 历史与版本 | 能力矩阵、未知内容保护、开放/停止/回退步骤 | 规则及候选保护已接入；profile 是最低能力声明，不冒充历史 APK 实测 |

完成判定逐项绑定实际提交和检查结果；单项测试通过不代表对应阶段全部通过。

## 3. 移动端保存与退出路径审查

`ThreadComposePage` 的发布、云端草稿保存/打开以及 `PostComposerSheet` 的提交/关闭均在继续前检查 `RichEditorSession.flush()`。主题离页路径中，原 `_flushSnapshot()` 返回 `Future<void>`；编码失败时直接返回，但 `_saveBeforePop()` 只检查本地存储失败状态，因此可能继续退出。

故障注入使用真实 `ThreadComposePage`、Quill 文档和 Codec：先保存有效正文 A，再模拟绕过字面来源保护的异常 producer 写入 `**待保存 B**`，独立确认编码确实抛出 `MarkdownCodecException`，随后触发系统返回。预期为保留页面、当前 Delta 和有效快照；连续返回不能丢稿；补齐正确来源属性后可保存并退出。

这是一条内部异常路径的可重复试验。尚未取得能由正常用户输入触发相同状态的原始复现，不能据此宣称已复现或修复用户过去遇到的丢稿问题。

## 4. 其他候选与独立反例

字面代码：共享 `rtb-literal-markers-whitespace` 原先把代码跨度里的 `<br />` 判作整行 HTML，导致打开后代码样式、可见文字不符。候选跳过完整代码跨度，关闭反引号必须整段长度相同；未闭合、错长及代码外 HTML 继续拒绝。旧共享用例红灯，候选及 9 个边界回归通过。

NBSP 阅读：同一用例的编辑/序列化已经通过，但阅读丢失段尾 NBSP。根因是 markdown `7.3.1` 的 `ParagraphSyntax.trimRight()`；适配器复用原段落解析，只恢复原文确有 NBSP 的被裁尾部，不抢占标题、列表或显式空段。共享阅读旧实现失败，候选及既有阅读组件测试通过。

撤销选区：原 Quill 按 Delta 长度估算位置，骰子替换撤销后原 `[0,1]` 选区变折叠，引用段首退格撤销后偏移多 1。共享预期独立来自操作前选区。候选把前后选区附着于原生历史条目，保留合并输入与选区方向；历史淘汰同步清理映射，替换文档清空映射，未经 Controller 的外部历史继续原生策略，不猜测选区。原共享回归失败，候选通过。骰子复制默认仍生成 UUID v4，仅增加测试用 ID 工厂观察实际复制后的身份。

原文保护：未知协议、原始 HTML、损坏节点或缺失对齐能力时，`RichEditorSession` 保留原 Markdown、只读展示并拒绝 flush；外部 `readOnly=false` 不能解锁。未改动的保护文档允许关闭，主题快照保留原文；改动过的异常文档不能走这一离页例外。原表格测试的“静默转义后保存”已被保护规则取代，另保留“未标脏仍检查当前有效文档”的独立回归。普通用户键入符号继续经字面输入路径保存。

实际 `PostComposerSheet` 覆盖编码错误、网络错误、`40002` 冲突与后端拒绝。失败保留当前编辑；编码失败无请求；冲突回读后明确覆盖才使用新版 version；恢复后提交当前正文。保存回执观察实际请求与假仓储持久化结果，不宣称公网 API 验收。

独立审查还发现图片方法缺只读防线、分隔线命令在历史记录结束后才更新最终光标、工具栏按长度误把等长表情替换当作没有编辑。回归已分别取到旧实现红灯；候选在图片入口拒绝只读，分隔线命令包含最终选区，异步工具只在文档签名未改变时恢复旧选区。没有跨 await 保持历史事务，也没有机械重写所有节点命令。

## 5. 结果复现与证据边界

统一入口仍是 `npm run check`；候选未部署时用 `npm run check -- -ContinueAfterFailure -BuildDebugApk` 收集后续结果，任一失败仍返回非零，不能关闭公网漂移门禁。

在已提交且干净的 Windows Worktree 执行 `pwsh -NoProfile -File tool/Export-RichTextBehaviorReport.ps1`。脚本运行四份共享测试并验证分片 SHA 和检查点唯一性，生成 `.dart_tool/rich-text-behavior/flutter-results.json`。编辑投影直接观察 Delta 和 UTF-16 选区；阅读投影读取已挂载组件的数据、语法及对齐配置，独立于 Codec；保存观测经过真实控制器/页面的仓储边界。没有以 fixture 预期填充实测值的回退。

报告绑定源码 SHA、fixture SHA、`platform=flutter`、`environment=unit-editor`；只使用固定脱敏语料，不收集真实正文、Token 或账号。交给 VPS Backend 候选运行 `pnpm contract:rich-text --compare <报告路径>`，校验 schema、完整性及首个差异字段。`canonical=null` 无当前阅读输入时显式 N/A，不能用旧快照或编辑 Delta 冒充阅读结果；其他未执行项目不能标通过。

契约逐字同步回归 1 项、21 个模块文档检查通过；OpenAPI 验证/重新生成无 diff。最终应用 analyze 零问题；固定候选的全量 Flutter 测试 2536 通过、1 项沿用已有跳过，36 项报告导出测试通过；Windows 工具测试 17 通过。架构、API 覆盖及生成客户端分析通过，Debug APK 构建和签名校验通过。

统一门禁首次以三项失败结束：公网 SHA 漂移、开发中静态问题、两个工具栏红灯。后两项已修正并分别以最终 analyze 和上述全量测试复核通过；不把首次日志改成绿色。公网仍为 `6bfb818df4ccf5333df7b62018a9f519d91e935b`，本地候选契约为 `fae818e8884813d476011dd936caa3463225c1a1`，部署一致性检查保持未通过。因此不能宣称 `npm run check` 整体通过，也不能把候选交付当作已具备上线条件。

## 6. 能力、开放与回退

三端回执已由主任务在 VPS 比较器复核（2026-09-10）：

| 消费端 | 绑定源码 | 实测 / 明确 N/A | PR |
| --- | --- | --- | --- |
| Flutter | `29ad5307923964498fb870c6a9f426692c6b3740` | 258 / 2 | [Mobile #18](https://github.com/morenk/wenyousite-mobile/pull/18) |
| Web | `c3cd6b905e2b0158adf643dba25b144e7bb9d647` | 249 / 11 | [Web #13](https://github.com/morenk/wenyousite-frontend/pull/13) |
| Backend | `6abb3a5315ef4f13e689eab4345352daa4adf0a4` | 63 / 2 | [Backend #12](https://github.com/morenk/wenyousite-backend/pull/12) |

Flutter 报告 SHA-256 `4e7003e21d4c9f321022dbcd974da42b39fbb0ec1ea487b5835e914e1c130a2e`，260 个检查点全量提交。Flutter 两个 N/A 均为编码失败无当前阅读输入；Web 另有两个本机快照关闭检查点（每点五个阶段）不适用于原本的 Web 关闭方式，但 Web 已单独验证关闭保护。比较器拒绝正常阅读标 N/A、缺失观测及旧快照冒充当前阅读。

Web 任务完整检查 2280 测试及生产构建通过，最终隔离候选浏览器回归 10/10；Backend 完整检查 1808 Jest + 17 离线测试通过。两端均为草稿 PR，未部署；Web 15 项真实 API 测试因未提供专用账号未执行。移动端导出脚本还验证了失败时旧成功汇总失效，避免误用上次结果。

| 声明 profile | 最低能力 | 不能据此推断 |
| --- | --- | --- |
| legacy-v3 | 基础正文 | 块/图片对齐及引用空行支持 |
| legacy-v4 | 基础正文、块对齐 | 图片对齐及新引用空行支持 |
| legacy-v5 | 基础正文及两类对齐 | newline revision 2 或无损保存保护 |
| candidate-v5 | 本轮语义、引用空行、保存保护 | 任一历史 APK 或真实设备已实测 |
| 未知/能力不足/转换失败 | 安全降级阅读、保留原文 | 允许创建该正文或覆盖编辑 |

开放顺序：先评审契约与候选回执，再经授权集成读取/未知内容保护，完成 S5 双向 API 保存和实际最低客户端验收，最后决定是否开放新增写能力。本轮不自动调整 `/meta`，只交付 PR 和候选 APK。

停止条件：fixture 哈希漂移；非 N/A 阶段缺失或失败；文字/空段/节点身份意外改变；未知内容被覆盖；只凭版本号放行缺失能力；公网部署 SHA 与计划不符。出现任一项即暂停受影响写能力开放，保留最后有效正文与输入证据。

回退前用待回退客户端读取候选实际写出的语料，核对文字、空段、节点和再次保存，不能只看旧进程可启动。旧 APK 未完成验证时不承诺安全回退；必要时经授权限制受影响写入口或要求升级。回退保留 Markdown，不批量改写历史、不删除草稿、不把保护改回自动转义保存。

负责人已明确段落内粘贴遵循目标段落对齐；跨段切片边界与最终光标仍待独立预期。引用转标题组合、列表/空格式块及任意随机操作不因本轮有限序列通过而视为全部覆盖。

## 7. 负责人复验与未执行项

候选 APK（2026-09-10 Windows 本地构建）位于 `build/app/outputs/flutter-apk/app-debug.apk`，对应应用源码 `29ad5307923964498fb870c6a9f426692c6b3740`。后续验收记录及缓存忽略更新不改变应用源码。

| 标识 | 实测值 |
| --- | --- |
| applicationId | `site.wenyou.app.debug` |
| APK versionName / versionCode | `0.7.0-dev.1-debug` / `94` |
| pubspec 版本 | `0.7.0-dev.1+94` |
| 最低 / 目标 API | 26 / 36 |
| 字节数 | 203735254 |
| SHA-256 | `1900c1886b63f2cc2844a448906258e8c3b29265b470b063dd925728a1740cad` |
| 签名 | `apksigner verify --verbose` 通过，v2 |

没有安装、操作设备或上传发布。构建输出的本地 `android/.kotlin/` 会话缓存加入忽略，避免污染源码状态；构建配置、依赖和签名未改。

候选应用包名为 `site.wenyou.app.debug`。复验中文输入法输入字面控制符、引用回车/退格/撤销、选中骰子复制和撤销、NBSP 与代码阅读、断网保存恢复、冲突明确覆盖、未知历史原文关闭。分别记录打开、输入、保存、重开和焦点/选区；按最终 APK SHA-256 确认安装对象。

内部编码故障使用测试注入，不要求负责人用普通输入稳定制造。原始 Bug 未提供实际输入时继续待复现。S5 的 Web→API→Flutter→API→Web 与反向旅程、实际 Android 输入法、设备操作及负责人明确验收尚未执行。自动测试、构建和代理自查均不替代负责人验收。

## 2026-09-11 合并整合

负责人明确授权本轮 PR 合并及按规范清理。整合时保留最新 `dev` 的列表结构、块边界、阅读复制和 Foundation v6.10.0，原候选缺失的源码能力不覆盖这些后续改进。原场景与 S5 的负责人验收状态保持，不以合并替代验收。

契约来源分别保留：HTTP/OpenAPI、列表及此前契约仍使用 `contracts/backend-contract.properties` 的 `062412601b3a8dbf4f64494115a2445d312dd53d`；本 PR 新增的三份 `rich-text-behavior-*` 文件来自已合并 Backend #12 的 `2a2847a829b24227b8b6e0930ef63af9b6436546`，与旧候选 `6abb3a5` 逐字一致。后端该 `dev` 尚不含列表候选语料，因此不能用它整体替换并删除移动端现有列表契约。下一次统一同步须使用同时包含这两组契约的已提交来源。

原已安装 APK（SHA-256 `1900c1886b63f2cc2844a448906258e8c3b29265b470b063dd925728a1740cad`）已另存于 Windows `D:/code/wenyousite/artifacts/rich-text-stability/20260910-candidate/app-debug.apk`，避免整合构建覆盖复验制品。

2026-09-12 凌晨整合验证：格式 963 文件无变化，应用与生成客户端分析零问题，架构、21 模块文档、API 覆盖及 Windows 工具 17 项通过。全量 Flutter 首轮 4313 通过、1 既有跳过，另 1 个测试文件在启动 WebSocket 连接时失败；单独重跑该文件的 1 项测试通过，因此回归合计 4314 通过、1 跳过。首次 API 再生成在旧缓存停滞，停止该生成进程并保留缓存后使用新缓存重跑，`npm run api:check` 返回 0，生成文件无 diff。

完整门禁原始非零结果保留：API 再生成与测试连接问题已按上述补验消除，公网精确来源仍为 `b785336c5b31cb228f3021650b9de1c39ade02e5`，与列表候选 `0624126` 不同；HTTP 均为 `5.20.1-dev.20260911.1`，Markdown v5 在兼容范围内。未部署，不宣称 `npm run check` 整体绿色。

最终追加整合包含已合并楼层定位 PR #24（dev `c8f4a109`）及阶段计划 #16；应用源码提交 `09e3dc6106cf92c192698b96b5aa6429c991d8b3`。最终完整静态分析零问题，7 个相关定位/页面测试文件的 71 项测试通过，21 模块文档检查通过；未重复已通过的全量回归。

Windows Debug APK 构建及 APK Signature Scheme v2 校验通过：`site.wenyou.app.debug`，`0.7.0-dev.1-debug / 94`，minSdk 26、targetSdk 36；大小 243709203 字节，SHA-256 `9515ab073259f96853a50e79da394d9e08d152ba1b7e4e02b2d410ac0b024bfe`。位于本 Worktree 的 `build/app/outputs/flutter-apk/app-debug.apk`。本轮未安装或发布该整合包；此前 ADB 安装证据属于另存的 1900c188 原候选，负责人原场景复验状态保持待验收。
