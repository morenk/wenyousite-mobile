# 移动端汇总复验候选

状态：候选／待负责人验收。负责人已明确允许将独立候选汇总到真机复验分支并通过 ADB 安装；不合并 `dev`、不发布。

## 包含的源码

| 范围 | 独立候选源码 | 证据 |
| --- | --- | --- |
| 行内代码、提及、收藏表情换行与基线 | `2cef83306702479ad38b734eb4001b61bb54beff` | [排版记录](inline-code-layout-acceptance.md) |
| 楼中楼可选回复目标校验 | `23cc47ea05988caa7fe5746e492a072541b8132c` | [分页记录](reply-pagination-acceptance.md) |
| 自动分页无手动按钮、失败底部重试 | `80c39491238245c3bb374b7b60bc1717ea89f410` | [分页记录](reply-pagination-acceptance.md) |
| 普通入口编辑后删除残留的目标缓存 | `29590f8bc6c6572cd2ee9817c787f63681955428` | [删除记录](deleted-floor-acceptance.md) |

共同基线为 `7d9ac403c758db5448423b974e3cd724998ea3df`，后端来源同步为 `27928edb4caac1e75b86292c2f8e4efe25a3565b`；Foundation 锁定已核对的最新正式 Tag `v6.9.0`。汇总仅解决变更记录的并列段落冲突，产品源码保持各独立候选内容。

## 失败反馈及版本核对

负责人反馈阶梯状错位仍在，保留“验收失败／继续排查”。设备前台确认为温油站 Debug，但已装 APK 哈希为 `6c5aa2263c4f5db00054c9f57f60594506744d4bda5552de2c2eb05c06875269`，仅含分页数据候选，不包含排版候选。此次组合明确同时包含这些源码，避免不同候选覆盖同一个 Debug 包后误判范围。

删除反馈发生在[原主题](https://wenyou.site/threads/cmtrvxfsy004s7q8a240qqb29)，由普通列表进入，先编辑再删除，残留原正文。实际页面保存后会增加 `post` 定位参数，删除与刷新遗漏目标缓存失效。完整操作链旧实现失败、候选通过；不使用请求成功提示代替页面移除断言。

## 验证及安装

完整 `npm run check:apk` 通过：格式、应用与生成客户端分析、架构、21 个模块文档、API 覆盖、公网契约与再生成一致性通过；2344 项 Flutter 测试和 16 项 Windows 工具测试通过，既有 Sentry 显式外部回执验证跳过 1 项；Debug APK 构建成功。依赖锁文件、生成客户端无额外差异。构建仍提示既有 `flutter_image_compress_common` 的 KGP 迁移警告，本候选未升级依赖。

- 应用：温油站 Debug，包名 `site.wenyou.app.debug`，版本 `0.7.0-dev.1-debug`，构建号 `94`。
- 本地文件：`build/app/outputs/flutter-apk/wenyou-debug-mobile-regression-39e32767.apk`，203697086 字节。
- SHA-256：`39e32767ecdbed6ac8f4dee0ee1f318d6c98ff15955e83d4949d43b6cf96f014`。
- 源码组合如上表，精确汇总源码提交见本记录所属候选提交和 PR；所有产品源码均在构建前固定。
- 安装前 AAPT 已确认 Debug 包名与版本；准备 ADB 更新时设备已断开，`adb devices -l` 为空，本包尚未安装。已请求负责人恢复连接，不能把先前 `6c5aa226` 的安装记录算作本包安装。

独立候选的旧实现失败证据见上表文档。未取得负责人真机验收，不关闭任何原问题。

## 本包复验步骤

1. 打开“温油站 Debug”，在原排版楼层对照编辑与阅读态，检查代码前后换行及阶梯状偏移；再检查提及与收藏表情换行。
2. 在原 76 条回复的楼中楼正序、倒序读到末尾，正常阅读无需手动展开；慢请求只显示进度，失败可在底部重试。
3. 在专用测试楼层按“普通入口 → 编辑保存 → 删除 → 下拉刷新”操作，确认原文立即移除且不再补回；删除失败仍保留内容。
4. 明确反馈各项是否通过；APK 构建、ADB 安装及自动回归不替代负责人验收。
