# 移动端汇总复验候选

状态：本轮原位删除与五项 UX 已于 2026-09-09 通过负责人真机验收。验收对应下方 `6b194c06` 更新包；仍未合并 `dev`、未发布。

## 已验收：原位删除与五项 UX

负责人反馈 `f4f05301` 删除后回到顶部，验收失败／继续排查，并要求处理全部五项审查发现。本轮在同一汇总分支改为删除已确认的单行、保留阅读窗口并仅刷新统计，同时实施五项反馈简化；[完整原因与边界](mobile-ux-feedback-audit.md)。下方各包记录保留为历史，新包检查及安装事实追加于末尾。

## 删除 UX 复验反馈与后续候选

负责人在下述 `39e32767` 汇总安装包上反馈正常删除出现红色不可见提示与重试，删除 UX 验收失败／继续排查。本轮在原汇总分支继续处理：简化确认、结束已删除目标定位、区分不可见与网络失败。七项页面回归旧实现四项失败、三项对照通过，候选七项通过。其他冗余 UX 的证据和待处理范围见[审查记录](mobile-ux-feedback-audit.md)。以下 APK 与安装数据保留为前一轮历史记录，新候选完整检查与制品另行追加。

## 包含的源码

| 范围 | 独立候选源码 | 证据 |
| --- | --- | --- |
| 行内代码、提及、收藏表情换行与基线 | `2cef83306702479ad38b734eb4001b61bb54beff` | [排版记录](inline-code-layout-acceptance.md) |
| 楼中楼可选回复目标校验 | `23cc47ea05988caa7fe5746e492a072541b8132c` | [分页记录](reply-pagination-acceptance.md) |
| 自动分页无手动按钮、失败底部重试 | `80c39491238245c3bb374b7b60bc1717ea89f410` | [分页记录](reply-pagination-acceptance.md) |
| 普通入口编辑后删除残留的目标缓存 | `29590f8bc6c6572cd2ee9817c787f63681955428` | [删除记录](deleted-floor-acceptance.md) |

共同基线为 `7d9ac403c758db5448423b974e3cd724998ea3df`，后端来源同步为 `27928edb4caac1e75b86292c2f8e4efe25a3565b`；Foundation 锁定已核对的最新正式 Tag `v6.9.0`。汇总仅解决变更记录的并列段落冲突，产品源码保持各独立候选内容。

## 失败反馈及版本核对

负责人反馈阶梯状错位仍在，保留“验收失败／继续排查”。设备前台确认为温油站 Debug，但已装 APK 哈希为 `6c5aa2263c4f5db00054c9f57f60594506744d4bda5552de2c2eb05c06875269`，仅含分页数据候选，不包含排版候选。此次组合明确同时包含这些源码，避免不同候选覆盖同一个 Debug 包后误判范围。

删除反馈发生在[原主题](https://wenyou.site/threads/cmtrvxfsy004s7q8a240qqb29)，由普通列表进入，先编辑再删除，残留原正文。实际页面保存后会增加 `post` 定位参数，删除与刷新遗漏目标缓存失效。完整操作链旧实现失败、候选通过；不使用请求成功提示代替页面移除断言。

## 验证及安装

完整 `npm run check:apk` 通过：格式、应用与生成客户端分析、架构、21 个模块文档、API 覆盖、公网契约与再生成一致性通过；2344 项 Flutter 测试和 16 项 Windows 工具测试通过，既有 Sentry 显式外部回执验证跳过 1 项；Debug APK 构建成功。依赖锁文件、生成客户端无额外差异。构建仍提示既有 `flutter_image_compress_common` 的 KGP 迁移警告，本候选未升级依赖。

- 应用：温油站 Debug，包名 `site.wenyou.app.debug`，版本 `0.7.0-dev.1-debug`，构建号 `94`。
- 本地文件：`build/app/outputs/flutter-apk/wenyou-debug-mobile-regression-39e32767.apk`，203697086 字节。
- SHA-256：`39e32767ecdbed6ac8f4dee0ee1f318d6c98ff15955e83d4949d43b6cf96f014`。
- 源码组合如上表，精确汇总源码提交为 `0cba53e5aafa14bea6230499aeebc991a5a5a599`；所有产品源码均在构建前固定。
- 安装前 AAPT 已确认 Debug 包名与版本；首次准备 ADB 更新时设备已断开，`adb devices -l` 为空，当时本包尚未安装。负责人恢复连接并明确要求安装后，本次 `adb install -r` 成功，已更新 `site.wenyou.app.debug`。
- 安装后已核对设备中的版本 `0.7.0-dev.1-debug`、`versionCode` 为 `94`、`lastUpdateTime` 为 `2026-09-09 01:13:22`；设备内 `base.apk` 的 SHA-256 与上述候选 APK 完全一致。负责人应打开“温油站 Debug”复验；先前 `6c5aa226` 的安装记录仅为历史记录。

独立候选的旧实现失败证据见上表文档。未取得负责人真机验收，不关闭任何原问题。

## 本包复验步骤

1. 打开“温油站 Debug”，在原排版楼层对照编辑与阅读态，检查代码前后换行及阶梯状偏移；再检查提及与收藏表情换行。
2. 在原 76 条回复的楼中楼正序、倒序读到末尾，正常阅读无需手动展开；慢请求只显示进度，失败可在底部重试。
3. 在专用测试楼层按“普通入口 → 编辑保存 → 删除 → 下拉刷新”操作，确认原文立即移除且不再补回；删除失败仍保留内容。
4. 明确反馈各项是否通过；APK 构建、ADB 安装及自动回归不替代负责人验收。

## 正常删除反馈更新包

本轮在上述汇总源码上继续处理正常删除被显示为故障的问题，其他 UX 审查发现尚未实施。完整 `npm run check:apk` 通过：应用和生成客户端分析零问题，格式、架构、21 个模块文档、API 覆盖、公网契约和客户端再生成一致性通过；2346 项 Flutter 测试、16 项 Windows 工具测试通过，1 项既有 Sentry 外部回执验证跳过。Debug 构建成功，仍有上述既有 KGP 迁移警告；生成客户端与依赖锁文件无变化。

- 应用仍为温油站 Debug，`site.wenyou.app.debug`，`0.7.0-dev.1-debug`，构建号 `94`。
- 文件：`build/app/outputs/flutter-apk/wenyou-debug-mobile-regression-f4f05301.apk`，203697086 字节。
- SHA-256：`f4f053014ee463cbd4968ee0be4c02d63a464a653c613aafdfc177439a7cd18c`。
- 重点复验：普通入口编辑保存后删除、直接定位后删除，只出现短成功确认，正文移除且下拉刷新不出现红色错误或重试；旧链接不可见为普通说明，临时网络失败仍可重试；取消和失败删除保留内容。
- 精确源码提交：`c33aeb250ee6a1e52c705286c714440df9cbce96`。本次 `adb install -r` 成功覆盖温油站 Debug，保留应用数据；安装后版本与构建号一致，设备 `lastUpdateTime` 为 `2026-09-09 02:05:56`，设备内 `base.apk` 的完整 SHA-256 与上述产物一致。
- 当前复验应打开“温油站 Debug”，使用本节 `f4f05301` 更新包；`39e32767` 是上一轮安装记录。所有原问题仍待负责人验收。

## 原位删除与五项 UX 更新包

本包继续保留全部排版、楼中楼分页候选，加入原位删除与五项 UX 调整。完整 `npm run check:apk` 通过：2357 项 Flutter 测试、16 项 Windows 工具测试通过，1 项既有 Sentry 外部回执验证跳过；应用与生成客户端分析零问题，格式、架构、21 个模块文档、API 覆盖、公网契约与再生成一致性通过。Debug 构建成功，生成文件和依赖无额外差异，仍有既有 KGP 迁移警告。

- 应用：温油站 Debug，`site.wenyou.app.debug`，`0.7.0-dev.1-debug`，构建号 `94`。
- 文件：`build/app/outputs/flutter-apk/wenyou-debug-mobile-regression-6b194c06.apk`，203697086 字节。
- SHA-256：`6b194c06934e9a85ff7cdc4e8ef0816d8572eb8b173885007633ab32ff1cca62`。
- 精确源码提交：`15d4b6b71054a35a5553cc6ac9f31baf26cecf17`。本次 `adb install -r` 成功覆盖温油站 Debug并保留数据；设备版本及构建号一致，`lastUpdateTime` 为 `2026-09-09 02:57:06`，设备内 `base.apk` 的完整 SHA-256 与本节候选一致。本包待负责人验收，当前应使用 `6b194c06` 包复验，上述 `f4f05301` 为上一轮记录。

复验顺序：

1. 在长帖中部删除本人测试楼层，确认相邻楼层填补且不回顶部；再检查最后一层、编辑保存后定位的楼层及取消删除。
2. 删除定位到的动态评论，确认提示简洁、取消保留、成功后没有红色不可见提示；旧链接仅显示普通不可见说明。
3. 慢速加载楼层时只显示进度，临时失败仍可在底部重试；不存在的主题保留返回入口，不提供无效重新加载。
4. 移动动态收藏失败只在选择弹窗提示一次，取消收藏失败仍有反馈；正文草稿保存／删除成功提示短暂退场，冲突仍可处理。
5. 原行内排版与楼中楼正倒序阅读仍按既有清单复验。安装和自动检查不替代负责人确认。

## 负责人验收（2026-09-09）

负责人在本轮安装交付后明确回复“验证通过”。本轮原位删除与五项 UX 验收通过：删除楼层保留附近阅读位置、动态评论删除与定位反馈、自动分页入口、主题不可见入口、动态收藏重复报错及草稿成功提示。

验收对应源码 `15d4b6b71054a35a5553cc6ac9f31baf26cecf17`，温油站 Debug 更新包 SHA-256 为 `6b194c06934e9a85ff7cdc4e8ef0816d8572eb8b173885007633ab32ff1cca62`。此前失败反馈与安装时的待验收状态保留为历史；本次仅追加验收记录，不重新构建或安装，不合并或发布。原行内排版与楼中楼分页的历史记录继续保留，本次未获得这两项单独的复验描述。
