# 图集切片的 Foundation 依赖同步

- 从正式发布源重新获取 tags，最新为 `v7.1.0`（`dcf75d385e39cc0c55d550a6f35921cd9a7aa508`）；保持 Schema 3，不消费未发布分支。
- 复用已提交的依赖迁移 `5bc62ed096b90659ed7e92555b00a76524e5a39f`：普通内容超过 72 小时或未来时间显示日期，读屏使用完整日期，精确记录时间不变。
- 从已审查提交 `51c70bfb5ef3ce962600a183cbc63f365fd0efa7` 复用 37 张日期视觉基线，以及主题/品牌版本断言。该提交的测试源码基线与本切片初始 `c5ba66683a6ad5004878d50fb19468211b261747` 在 `test/features` 中无差异；未带入图片上传实现及其 HTTP 契约。
- 日期基线的既有差分核验仅涉及作者日期行；小屏大字下日期缩短恢复作者名可见空间。图集功能的最终完整门禁仍须重新检验这些基线。
- 本切片已运行相对时间、身份时间读屏、主题和品牌资产四个测试文件，共 14 项通过；`flutter pub get` 通过。

此依赖迁移与新增图集接口分别提交，便于与其他并行切片合并时识别重复升级。图集候选的设备验收另行记录，不以依赖检查替代手势验证。
