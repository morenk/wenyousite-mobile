# 富文本 S5 跨端与真机验收

状态：进行中。候选已通过 ADB 安装及设备内哈希核验；双向真实 API 旅程与负责人功能复验尚未完成。

## 1. 授权与候选

2026-09-10 负责人要求推进 S5，并明确真机部分由代理执行 ADB 安装。代理不代替负责人确认页面、输入法和原始问题已验收通过。

沿用 [S1～S4 执行记录](rich-text-stability-execution.md) 的候选：Mobile 应用源码 `29ad5307923964498fb870c6a9f426692c6b3740`、验收记录基线 `ebb5d03f89ead207a57c5ead8cd7279ba55f440d`；Web `c3cd6b905e2b0158adf643dba25b144e7bb9d647`；Backend `6abb3a5315ef4f13e689eab4345352daa4adf0a4`。S5 使用独立 Windows Worktree/任务分支，候选源码没有重建或替换。

开始核验时 Mobile #18、Web #13、Backend #12 均仍为 OPEN、未合并。公网 `/api/v1/meta` 返回 Backend `6bfb818df4ccf5333df7b62018a9f519d91e935b`、HTTP `5.18.0-dev.20260905.1`、Markdown 5，推荐 Android build 93。候选尚未部署，不能将公网 Web 当成候选 Web，也不能把测试夹具回执称为真实 API 验收。

## 2. ADB 安装证据

| 项目 | 实测 |
| --- | --- |
| 设备 | Android 型号 `2509FPN0BC`，ADB 状态 `device` |
| 目标应用 | `site.wenyou.app.debug`，温油站 Debug |
| 安装前 | 正式版与 Debug 同时存在；Debug 上次更新时间为 2026-09-09 15:39:15 |
| 安装方式 | 对指定设备执行 `adb install -r`，结果 `Success`，保留应用数据 |
| APK versionName / versionCode | `0.7.0-dev.1-debug` / `94` |
| 安装后更新时间 | 2026-09-10 04:10:59，设备时区 +0800 |
| 本地与设备内 APK SHA-256 | `1900c1886b63f2cc2844a448906258e8c3b29265b470b063dd925728a1740cad`，相同 |

安装包来自 Windows `mobile-rich-text-stability/build/app/outputs/flutter-apk/app-debug.apk`。安装后通过 `pm path site.wenyou.app.debug` 获取实际 base.apk，并在设备内执行 `sha256sum`；不是仅凭构建号相同推断已更新。正式版没有被覆盖，没有卸载、清数据、代操作页面或发布安装包。

## 3. 双向旅程

只使用专用普通测试账号及本轮新建的测试内容，标题标注“富文本 S5 测试”与日期。不改负责人原帖，不批量清理，不注销账号；密码/Token 不写聊天、仓库或报告。

| 旅程 | 顺序与观察 | 当前结果 |
| --- | --- | --- |
| Web 起始 | 候选 Web 工具栏创建 → 真实 API 保存 → Debug App 阅读/打开编辑 → 输入/保存 → 候选 Web 重开 | 待执行 |
| Mobile 起始 | Debug App 工具栏创建 → 真实 API 保存 → 候选 Web 编辑/保存 → Debug App 重开 | 待执行 |
| 草稿 | 当前正文保存云草稿 → 另一端打开 → 编辑后保存 → 原端重开 | 待执行 |
| 失败保护 | 断网保存保留正文，恢复后重试；两端编辑冲突回读后明确覆盖 | 待执行 |

每轮记录候选客户端 SHA、目标 API SHA、测试对象 ID、操作顺序、保存前后 Markdown 及独立可见语义。只记录固定测试内容，不采集实际用户正文。页面操作、实际 HTTP 往返、阅读与编辑结构分别核对；内部编码故障继续使用既有受控测试，不把异常属性注入当成正常用户操作。

已请求 VPS 关联任务检查已有 E2E 配置、真实登录入口以及隔离候选连接实际 API 的可行性；本次任务返回未包含可核验结果，不能记作联调通过。若没有专用账号，等待负责人提供本机/VPS 凭据配置路径或通过正常登录流程建立测试会话；不得以数据库直写、读取个人会话或绕过验证码替代。

主任务已确认：Backend 公网提交到候选提交的 `src/prisma` 差异只有新增测试 `rich-text-write-contract.spec.ts`，没有发现这两个目录下的生产逻辑/迁移差异；这不等于候选已经部署。Web 现有 Playwright 配置要求显式 `E2E_ENV=test`，前后端地址仅接受 loopback，因此不能直接把其 15 项用例指向公网域名。当前 SSH 执行环境的 `E2E_EMAIL`、`E2E_PASSWORD` 均未设置（只检查是否存在，未输出凭据）；其他私有配置是否可用仍待关联任务/负责人确认。联调须先确认合法候选入口与专用账号，不能为运行测试绕过原有目标地址限制。

## 4. 负责人 Debug App 复验清单

以下各项都需要在上述已核验 APK 上操作，结论目前均为待验收：

1. 普通输入 `**甲**`、`> 乙`、`<br />` 保持字面文字；通过工具栏应用粗体、引用与行内代码，保存后重开不泄露星号或反斜杠。
2. 正文/引用中的回车、真正空段、引用段首退格及撤销/重做，检查段落归属、空段数、最终光标和原选区。
3. 选中骰子复制粘贴，检查新节点身份、替换撤销；选中文字用表情替换后，光标不恢复成旧选区；分隔线重做恢复最终位置。
4. 中文输入法联想、选词、长正文移动光标后删除/继续输入；真实输入法行为由负责人复验，测试输入连接的模拟不替代它。
5. 代码内显示 `<br />`，段尾不换行空格保持；分别看阅读、打开编辑和保存重开。
6. 失败不退出、不显示伪成功；未知历史内容只读保留原文，未修改时可关闭。

负责人反馈时绑定操作、预期/实际和该候选包；若原问题仍在，记录“验收失败／继续排查”，保留本次安装证据。只有负责人明确通过后，才能把对应 Bug 从候选改为完成。
