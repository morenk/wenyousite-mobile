# 轻量交互乐观更新候选验收

状态：负责人确认通过并授权合并清理。2026-09-13，负责人在候选交付后明确回复“可以合并清理分支了”；对应实现提交 `7182a6c4` 和下述 SHA-256 的 Debug APK。未提供逐项真机操作日志，不补写具体设备、场景或测量结果。

## 目标与基线

- 用户要求跳过已完成的收藏表情拖动排序，补齐其余适合乐观更新的交互。
- 原行为：点赞、收藏、关注、订阅、列表移除和归档等待写请求完成才改变可见状态；收藏弹层等待写入成功才关闭。通过延迟完成请求，在返回前断言可见状态，独立验证旧实现与候选差异。
- 从 `origin/dev` 的 `993934f0` 建立独立 Windows Worktree，分支 `codex/20260913-optimistic-interactions`。未修改主工作区或其他任务。
- 后端固定 revision 与最新 `origin/dev`、公网 `/meta` 均为 `6fdfa00eaf1f3056ba30f2ffbc529d12eed1c823`，契约 `5.22.0-dev.20260912.2`；无契约改动。
- 已 fetch Foundation Tag，最新正式 Tag 为 `v7.0.0`，沿用最新移动端基线已有的固定依赖与系统字体迁移。本任务不重新声明字体验收通过。

## 行为与边界

- 主题及动态点赞/收藏、关注立即改变选中态及计数；成功采用确认值，失败恢复本次字段。官方/玩家订阅显示待提交选择，保留真实记录 ID。
- 收藏夹选定后立即关闭弹层，成功提示仍等待确认；失败恢复状态并在原页面提示，可重新打开选择器。
- 主题和动态收藏移动/取消立即移出当前目录；主题目录同步调整数量；动态目录独立重新读取，禁止用分页长度替代总数。
- 删除通知立即移除并扣减对应角标；移除收藏表情同时更新收藏和最近使用，失败原位恢复。表情排序未改动。
- 归档仅乐观投影归档字段；阅读私聊扣减本会话已读部分，不能扣减待处理请求。旧计数查询作废，请求结算后重新读取包含新消息的计数。
- 同一目标保留写锁；动态不同卡片可独立提交。迟到读取和失败回滚不得覆盖新状态。退出、切号、页面释放仍由原作用域失效规则保护。
- 超时等结果不明时先回读；分页缺少目标不视为删除成功。拉黑、权限任免、撤回、钱包扣款及发帖流程不在此切片。

## 自动验证

- 旧实现对照：临时恢复七个相关控制器到基线，运行 13 个“立即变化”回归，全部失败；恢复候选后同一组 13 项全部通过。测试保留原请求的延迟完成，不以本实现的输出构造预期。
- 首批控制器与实际页面回归 87 项通过，覆盖选中态、订阅投影、收藏列表/目录数量、通知及表情移除、归档与角标、旧刷新、并发失败恢复，以及收藏弹层关闭和重试。
- OpenAPI 校验、再生成一致性、公网 `/meta` 与公开主题 GET 兼容检查通过；生成客户端及依赖锁文件无改动。
- 最终补充回归 24 项通过，覆盖动态详情互动与评论并行、结果不明时回读确认且不重复写入，以及归档期间会话失效后丢弃迟到响应。
- 通知断网补充回归：删除失败且角标回读失败的用例在补充前失败（期望 2、实际 1），补充后通知控制器 14 项全部通过，覆盖本地计数恢复、删除期间暂停旧计数回读、只结算一次及后续全部已读保护。
- 最终冻结源码的 `flutter build apk --debug --no-pub` 通过；完整门禁 `npm run check -- -TestConcurrency 4` 通过，包含格式、应用与生成包静态检查、架构、文档、API 覆盖与契约检查。
- Flutter 全量测试 4,392 项通过、1 项跳过、0 失败；跳过项为需显式开启的真实 Sentry 回执验证，未提供本次在线验收配置。Windows 发布工具测试 17 项全部通过。
- 无专用账号业务写入冒烟、无真机安装与负责人验收结论。

## 候选 APK

- 构建：`build/app/outputs/flutter-apk/app-debug.apk`；应用名称“温油站 Debug”。
- 包名：`site.wenyou.app.debug`；APK versionName `0.7.0-dev.1-debug`，versionCode `94`；最低 API 26。
- SHA-256：`921f82cd260def7d7a36dcc7871a403406dbc13daa0ee3994260879450ef144b`。
- 编译保留已有 `flutter_image_compress_common` 的 KGP 迁移提示；本次构建成功，插件升级不在此切片。

## 合并复核与留档

- 2026-09-13 合并最新 `dev`（`f99996a0f28d673529108341a387da6852fbcf78`，以合并提交 `187785bf` 的第二父提交核验）：文档保留乐观更新与完整媒体展示两组行为；私聊发送保留媒体处理中状态和失败映射。未读角标 Provider 移至对应控制器文件，保持作用域依赖和原入口导出，满足单文件行数门禁。
- 受影响的私聊、社交、通知、动态、表情及乐观更新回归 243 项全部通过；架构、文档和合并后 Debug 构建通过。全应用静态检查仅发现两处导入问题，修正后私聊 application 范围复核零问题，格式检查通过。此前完整门禁结果对应原候选实现，不混称为本次合并后的全量回归。
- 原候选 APK、合并后 APK 与测试日志保存在 `D:/code/wenyousite/artifacts/mobile/20260913-optimistic-interactions`。合并后包名仍为 `site.wenyou.app.debug`，文件 `app-debug-merged-dev.apk` 的 SHA-256 为 `499ba03216769518ef5c9964fafb817a21d03e6527e9cef17adae917628eed8f`；该新增构建未另行声称逐项真机验收。

## 负责人真机检查

使用公网专用测试账号；仅操作本人本次创建的测试内容，不批量删除共享数据。

1. 正常网络及弱网下，对主题、动态点赞/取消、收藏/取消，检查图标与计数立即变化；快速重复点击不会发出重复写入。
2. 关注与取消关注、官方更新与玩家订阅立即切换；不同动态并行操作，一项失败不能恢复其他项。
3. 新增收藏和移动选择目标收藏夹后，弹层立即关闭；确认前不显示成功提示。失败能恢复卡片/状态、看到提示，并重新选择后重试。
4. 从主题及动态收藏夹移动、取消收藏，检查立即移除、原目录及目标目录计数最终正确、分页无丢失；失败回到原位置。
5. 确认移除一个测试表情，检查收藏和最近使用同步消失；断网失败恢复。复核已有拖动排序与连续拖动不退化。
6. 删除本人一条测试通知，检查即时消失和未读数变化；快速切换筛选后旧请求不能把列表切回去。
7. 归档/取消归档立即改变状态；失败恢复。打开有未读的已接受私聊，角标立即减少；同时让另一测试账号发新消息，最终数量正确；查看陌生请求不会将请求视为已接受。
8. 操作期间返回、切换页面或退出测试账号，旧操作不能覆盖新页面/新账号。重开页面后与权威状态一致。

候选应用包名为 `site.wenyou.app.debug`。本任务未执行 ADB 安装；负责人已在本次对话确认通过并授权合并清理，以上步骤保留为回归检查清单。
