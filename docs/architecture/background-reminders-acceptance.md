# Android 常驻与横幅消息提醒候选验收

状态：负责人验收通过（2026-09-13）。负责人在最终候选 `bac2746e` 上明确确认并授权合并清理，见[最终验收记录](notification-read-acceptance.md)。以下保留排查及候选历史；本次确认没有逐项提供 30 分钟、Doze 等扩展场景数据，不推断所有扩展检查已执行。深度休眠可能延迟送达的边界不变。

2026-09-13 后续反馈：负责人确认消息卡片可到通知栏，手动开启小米“新消息提醒”的悬浮通知后已有顶部横幅；这只确认该场景的横幅开关原因，不代表 30 分钟、Doze、划掉任务等完整清单通过。截图中消息频道声音为“无”、振动关闭、锁屏不显示；新要求为完善首次授权/设置引导及默认系统提示音，见[细节候选记录](notification-guidance-acceptance.md)。

## 原始问题与证据边界

负责人反馈：此前系统顶栏消息提醒一直没有到达，“一秒钟都没能坚持”。预期为离开温油站后仍能收到站内通知和私聊；实际为未收到。原问题具体安装包、通知权限、锁屏时长和发送时间暂无完整记录，也未取得原场景的系统日志。负责人已另行授权本次候选构建后直接 ADB 覆盖安装；这不等同于原场景复验通过。

已证实：旧实现只有 Dart 进程内 30 秒 Timer，Android Manifest 不声明前台服务，无法保证后台执行。尚未证实：原现场是否确由进程冻结、频道设置或其他环节导致。自动测试不替代原现场复验。

## 已确认边界

默认开启且设备级可关闭；仅后台常驻，每 30 秒检查；返回前台、退出登录、划掉任务停止；重启不自动恢复。前台服务使用 specialUse，当前面向 RainS3 APK 分发。接受深度休眠延迟，不接入 FCM、电池优化豁免、WakeLock、精确闹钟或开机自启。顶部弹窗为系统横幅，不包含悬浮聊天窗口。

## 基线

2026-09-13：Mobile origin/dev 为 f99996a0，工作在独立 Windows Worktree。Backend origin/dev 与公网 /meta 均为 6fdfa00eaf1f3056ba30f2ffbc529d12eed1c823 / 5.22.0-dev.20260912.2；公网 Markdown v5，移动端兼容集保持 3/4/5。Foundation fetch tags 后最新正式版为 v7.0.0，与锁定版本一致，无需同步依赖或生成契约。

## 实现与风险

- 设备开关与 Android 执行桥接不保存凭据。HTTP 继续使用现有 Flutter isolate、会话刷新锁和账号仓储；服务仅维持前台执行资格、常驻卡片及权限状态。
- 原生 onPause 在 super 之前申请服务；返回前台撤销启动代次，迟到的旧服务请求不能复活。服务明确 running 后才允许基线和轮询；服务失败、通知被关闭、回前台或账号变化均取消当前代次，展示前再次核对状态。
- 原消息频道 wenyou_messages_v1 不更换、不删除，用户降低重要性仍允许普通通知；wenyou_background_reminders_v1 为静音低重要性频道。两者都不绕过勿扰或锁屏设置。
- 常驻服务不等于永久后台存活。厂商调度、Doze、断网、用户停止应用或进程被回收均可能导致延迟或停止；不宣称具有 QQ/微信相同送达能力。

## 本地验证记录

- 首轮 Kotlin 编译发现 Intent.flags 与 onStartCommand 参数同名，改为 this.flags 后 Debug 构建成功；测试中平台插件命名参数与生命周期回调的 void 用法也已修正。这些构建前问题不作为已交付版本。
- 真实 AppScaffold 生命周期回归先证实：仅监听派生 SessionScope 时，后台无新帧退出未立即撤销执行资格。改为直接监听认证源后，同一测试通过；读取账号作用域也直接采用认证源快照。这证实了当前候选开发中的竞态，不等于证实原手机“不推送”的全部根因。
- 偏好默认/持久化/读写失败、网关状态桥接、服务确认/失败/停止、30 秒不重叠调度、旧请求失效、基线去重重试、汇总、私聊隐私、严格目标、频道重要性与设置面板布局均有自动回归。
- 原生服务静音配置、权限声明、START_NOT_STICKY、stopWithTask 与合法启动窗口为源码静态回归；真实系统横幅、声音、振动、Doze 和厂商服务管理必须以下述真机测试为准，不能用静态断言替代。
- 将 f99996a0 的旧 poller 与当前“后台不读取通知未读数”回归复制到隔离 build 目录执行，确认旧实现因两次 fetchUnreadCount 调用而失败，当前实现通过；未修改工作源码或扩大到共享后端数据。
- 完整门禁、最终候选 APK 与安装身份见本记录后续交付条目。
- 首次完整门禁在分析阶段发现 build 目录中旧实现对照测试副本的 import 排序问题；该副本只用于确认旧实现失败，已移除，保留失败日志。没有关闭 lint 或放宽检查，完整门禁重新执行。
- 随后旧基线完整门禁已通过分析、契约、架构、文档及 3664 项测试（1 项既有跳过），尚未跑完时发现 dev 已合入负责人验收的乐观更新 PR #31。为避免交付旧体验，停止该轮剩余测试并保留日志，不将这轮记为完整通过。
- 任务分支整合至 origin/dev e5eea0de；仅 CHANGELOG 的两个新增记录需人工并列保留，业务源码无冲突。整合后的功能提交为 a1af3d3d，重新通过统一入口 npm run check -- -TestConcurrency 2 验证并构建；未合并 dev、打 Tag 或上传 RainS3。

## 负责人真机手测清单

仅使用专用测试账号。打开本候选对应的“温油站 Debug”，不要误开同时安装的正式版。

1. 在账号设置确认“后台消息提醒”默认开启；打开“系统消息通知设置”，确认进入“新消息提醒”频道，允许消息通知与横幅。
2. 回到温油站后切到其他应用，确认立即出现“后台消息提醒已开启”常驻卡片且不响铃、不振动、不弹横幅。首次建立基线不补发历史消息；等首次基线完成后再发送测试消息。
3. 连续后台至少 30 分钟，期间分别发送站内通知与私聊，至少覆盖开始、中段与第 30 分钟附近。记录每次发送/到达时间、屏幕开关与网络条件；消息横幅收起后卡片仍在通知栏，私聊只含用户名与通用提示，不出现正文。点击通知、私聊分别进入准确目标。
4. 回前台确认常驻卡片收起、前台不再发后台横幅；再次后台确认恢复。快速来回切换数次，检查没有遗留卡片、重复轮询或启动失败提示。
5. 分别关闭横幅、降低消息重要性、关闭声音/振动：普通消息卡片仍可收到；开启勿扰与锁屏隐藏内容时服从系统选择。关闭全部应用通知或消息频道后停止检查；回前台重开并重新后台可恢复。另验证关闭常驻频道时停止并提示。
6. 关闭应用内开关，确认常驻停止；重开应用、退出与切换测试账号后开关选择仍保留，旧账号在途结果不能发卡片。重新开启后可再次后台运行。
7. 从最近任务划掉应用，确认服务与常驻卡片停止；手机重启后不自动出现，必须再次打开应用。此后新后台时段重新建立基线，不补发历史提醒。
8. 锁屏静置进入深度休眠，记录期间发送时间；恢复联网/唤醒后验证仍运行服务的检查能够继续，允许休眠期间延迟。不自动申请豁免或绕过休眠；若服务已被系统终止，回应用重建并记录系统行为。

尚未验证：上述真实横幅/通知音/振动、锁屏与勿扰组合、30 分钟双消息送达、任务划掉和深度休眠恢复，Android 8 与当前 Android 16 的原生行为差异。负责人未明确通过前，原问题继续保持待验收。

## 候选安装包身份

- 本地版本：0.7.0-dev.1+94；APK versionName 为 0.7.0-dev.1-debug，包名 site.wenyou.app.debug，应用名“温油站 Debug”。不覆盖 site.wenyou.app 正式版，不执行数据清除或卸载。
- 整合前阶段 Debug 构建及 apksigner v2 签名验证通过，SHA-256：733dfdcbcc6bf853482a663569125a49fec1d6a7fc45995bb4eca6ef8477220c；该阶段包未安装，不作为最终候选。
- 最终整合候选来自 a1af3d3d（基于 e5eea0de），Debug 构建及签名验证通过；SHA-256：9bce7774043b2c581c92fd1c22cb5394e0c3c1c74a6e31e2405114b49fec7ba1。包名与版本仍如上，必须核对本哈希。
- aapt 对实际 APK 的权限检查确认新增 FOREGROUND_SERVICE / FOREGROUND_SERVICE_SPECIAL_USE，消息振动使用既有 VIBRATE；未混入 WAKE_LOCK、RECEIVE_BOOT_COMPLETED、精确闹钟、悬浮窗或电池优化豁免权限。
- 安装前确认连接的 2509FPN0BC 为 Android 16 / API 36，近期温油站进程为 Debug 包；旧包更新时间 2026-09-13 02:09:56，同为构建号 94，但 SHA-256 为 50b20bfd782c4d17581f8b6dbaf0fd096792db388934577aa9b5ebf397f146fd。因此本次以包名、实际 APK 哈希与更新时间共同确认覆盖，不能仅凭构建号判断。
- 针对性回归 47 项通过。最终源码 a1af3d3d34ea432bf0e9ab9f26a9b0d568268686 的完整 npm run check -- -TestConcurrency 2 通过：Flutter 4463 项通过、1 项既有显式 Sentry 联网验收跳过，Windows 发布工具 17 项通过；格式、应用/生成客户端分析、架构、21 模块文档、API 覆盖、生成一致性与公网契约核验均通过。完整日志保留在本地 build/background-reminders-check-integrated.log。
- 2026-09-13 03:38:35（Asia/Shanghai）按负责人授权执行 adb install -r，返回 Success。再次读取 site.wenyou.app.debug 的包路径、版本、更新时间及设备内 base.apk SHA-256，确认与最终候选 9bce7774…9fec7ba1 完全一致。安装前旧版本号同为 94，实际哈希及更新时间均已变化；未卸载、清数据或覆盖正式包。
- 仅完成安装与包身份验证，未代替负责人执行真实双消息、横幅/勿扰/锁屏与连续 30 分钟验收；状态继续为“候选／待负责人验收”。
