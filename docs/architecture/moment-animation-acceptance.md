# 动态帖动态图规则验收

状态：负责人验收通过／修复完成。实现基线为 Mobile `dbba204`，Foundation 固定正式 `v6.9.0`；不依赖未发布 Tag。

2026-09-11，负责人在已通过 ADB 安装的候选源码 `55a81be774544f7d591835cf2a43d4781b42f863`、Debug APK SHA-256 `ba4fb0905eee35881a3106fc9349b372f9b8ba9aa0785e83dbae2545da83cd18` 上复验后，明确回复“验证通过了，该合并部署了”。本记录依据负责人的验收反馈，不声称代理亲自完成真机、相册或 TalkBack 逐项测试。验收后仅更新文档，候选生产源码及 APK 未变化。

## 行为与证据

- 列表统一经过 `MomentCoverImage`，GIF（包括 `animated=false` 但 `contentType=image/gif` 的旧记录）只允许 thumbnail；静态图片只允许 feed、thumbnail、medium。缺少派生图或失败显示占位，URL 与原图相同的候选也排除。
- 详情当前轮播图、可见主评论与楼中楼图片／动态表情自动播放；所有祖先滚动视口取交集，路由覆盖、非当前轮播页、App 非前台卸载动画图片。
- 动态表情 medium 与 url 可能相同，静态预览明确使用 thumbnail。评论表情也可打开原图；原有长按菜单保留。
- 全屏沿用缩放、下滑关闭和当前图片操作；仅当前动画加载。详情及全屏共用失败记录，失败后只有显式重试可以再次播放。保存 GIF 使用原始字节且无静态回退。
- `moment_animation_test.dart` 覆盖真实红／蓝双帧 GIF 的像素变化、列表不请求原图、暂停静帧、选中轮播、全屏进退、同屏评论／楼中楼、离屏／前后台、缺图／失败和显式重试。原动态页面 Golden 与图片收藏来源回归沿用原基线。
- 后端语义回归：[Backend PR #16](https://github.com/morenk/wenyousite-backend/pull/16)，提交 `15b7030e968cd8dd8941ab6f8fc15610e30b57e8`；共享验收规则：[Foundation PR #8](https://github.com/morenk/wenyousite-foundation/pull/8)，提交 `d2577ed1f441be0997021857e24c9076adaef006`。二者仅为已提交评审事实源，HTTP/OpenAPI 不变。

## 自动检查记录

- 新增 8 条组件／资源回归通过：独立缓存和可控 HTTP 503→200，直接读取真实 GIF 解码后的 RGBA 像素；同时检查原图请求数、100×50 静态图片／表情尺寸及两层滚动视口。不把 Foundation 的 33 条共享 fixture 声称为逐例执行，本次用例覆盖对应关键行为。
- 旧实现反证：从基线 `dbba204` 原样提取 `MomentCoverImage` 到 `.dart_tool` 的临时探针，以同一缩略图 503 用例运行，确实额外请求动画原图而失败；候选组件通过。没有为验证修改主工作区或主题帖实现。
- `npm run check` 的 OpenAPI 校验、生成客户端再生成零差异通过。公网核对曾因 VPS 故障中断，恢复后独立重跑通过：API／bundle `5.20.0-dev.20260909.1`，build `0ee2c0de1d9c570e495e778be6661b074b7a4bef`，GET `/threads` schema 兼容。
- 剩余本地门禁按同一入口原参数继续：格式、生成包 analyze、架构、21 个模块文档及 API 覆盖 154/154 通过。应用 analyze 首轮发现两项测试 import lint，修正后完整复验通过，最后两个源码文件的增量 analyze 也通过；保留初轮失败记录，不将中断的单次命令称为全绿。
- 全量 Flutter 测试 2487 条通过，1 条需要显式 `WENYOU_VALIDATE_SENTRY` 的外部诊断接收验收按原设计跳过；发布工具测试 17/17 通过。
- Debug APK 构建通过：`site.wenyou.app.debug`，`versionCode=94`、`versionName=0.7.0-dev.1-debug`，文件 `build/app/outputs/flutter-apk/app-debug.apk`，大小 `203780034` 字节，SHA-256 `ba4fb0905eee35881a3106fc9349b372f9b8ba9aa0785e83dbae2545da83cd18`。未安装设备，不能视作设备验收。

## 负责人设备复验

首次候选交付时没有连接的 Android 设备，以下为当时交付的复验清单。随后已安装候选，负责人明确验收通过，记录见本文开头；自动化不能替代该人工结果。

1. 使用专用账号分别查看发现、关注、搜索、个人主页和收藏的同一 GIF：正常封面、缺缩略图、缩略图错误时均静止。
2. 详情验证单图和多图；切图仅当前播放，滚到评论区后轮播停止，同屏主评论与楼中楼 GIF／动态表情都播放，离屏停止。
3. 打开全屏、切图、双击／捏合缩放、下滑／返回关闭；底层动画暂停，关闭后恢复。临时离开页面、锁屏、切到其他 App 再返回核对恢复。
4. 断开图片源制造动画失败，确认静态预览和“重试播放”；切图、全屏进退、前后台不自动重试。恢复网络后明确重试。
5. 保存 GIF 后在相册确认仍有多帧；主图失败不能以静态预览冒充保存成功。收藏当前动态／评论图片后确认来源正确。
6. 回归 100×50 静态图片、横竖图、动态发布编辑器和主题帖；核对原比例、静态图片尺寸与原路径一致。

## 交付边界

负责人已授权 PR #25 合并到 `dev` 并清理本任务临时分支与 Worktree。验收 APK、日志和本记录归档到治理工作区 `artifacts/mobile-moment-animation-pr25`。本次不发布正式 APK、不晋级、不发 Tag，也不操作设备；远端服务部署由治理任务另行执行。
