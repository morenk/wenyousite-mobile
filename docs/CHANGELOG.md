# 移动端变更记录

## Unreleased

### Added

- 同步后端契约 `4.9.0-dev.20260814.2`：生成移动端管理员使用普通 Bearer 直接隐藏内容的端点；先登记为 moderation 待接入能力，在完成可信角色投影、高风险确认和内容状态刷新前不展示入口。
- 同步后端契约 `4.9.0-dev.20260814.1`：生成当前隐藏内容管理端只读接口；该接口仅使用站务 Cookie 会话，移动端 V1 明确登记为不适用，普通用户流程不变。
- 同步后端契约 `4.8.0-dev.20260813.2`：生成用户主页 3:1 背景图设置/移除与活动汇总客户端；移动端先登记为待接入 users 切片，不提前展示未完成入口。
- 内容输入统一为键盘上方 dock：主题创建使用固定元信息和剩余正文画布，楼层/回复使用可扩展半屏，动态/评论/私信共享底部操作语义；富文本标题和更多能力在编辑器内展开并保持 WYSIWYG 选区。
- 主题列表卡片在现有整宽封面和卡片层级中加入作者头像，非法或失败头像使用统一占位且不增加卡片信息层级。
- 同步 Foundation v1.2.1 跨端列表语义：列表容器与列表项固定占满分配列，消息气泡、标签、徽标和紧凑操作保留内容宽度例外；主题详情楼中楼等级徽标恢复内容宽度，排头重排为单主面板与 360dp 同行工具栏。
- 接入后端 `4.7.0-dev.20260811.1` 完整申诉闭环：普通登录可读取本人近 30 天决定，被暂停或封禁账号可用密码换取仅存页面内存的 15 分钟专用凭据，查看决定并提交一次申诉；`40120` 只清专用凭据，不影响普通会话。
- 动态正文与评论消费 `wenyousite-internal-reference` v1 黄金契约：只激活显式或裸站内主题坐标，普通 Markdown 与外链保持字面文本，并支持主题、指定子贴、楼层、讨论和具体回复应用内导航。
- “我的收藏”新增私有收藏夹分类：按“全部 / 默认 / 自建”横向筛选并展示服务端计数，支持 1–24 字新建分类和移动收藏；移动端使用底部选择器适配窄屏，新建后自动选中，分类错误不遮断已有收藏列表。
- 动态发布与编辑新增本机自动草稿：文字、封面和图片顺序防抖保存，重新进入可恢复或放弃，离页前明确保留/放弃，发布动作固定在底部安全区。
- 动态、主题与楼中楼详情新增统一讨论入口：游客原位登录，普通评论和定向回复带入真实目标，楼层/回复继续复用 Quill 底部编辑器；动态评论编辑器补齐软键盘避让和未发送内容离开确认。
- 新增可重复安装的 Windows Android 一键发布工具：RainS3 专用密钥使用当前 Windows 用户 DPAPI 加密并收紧 ACL，SSH 首次接入必须与 VPS 控制台 ED25519 指纹一致；桌面入口自动读取 `pubspec.yaml`，仅允许干净且已推送的提交，并串联测试、验签、RainS3 上传、VPS 晋级和线上 `/meta` 复核。
- 创建主题页新增服务端完整草稿箱：`threadsFindDrafts` 展示跨设备未发布主题，继续编辑前以 `threadsFindById` 二次确认本人、未发布事实和全部乐观锁版本；其他草稿可二次确认删除，切换前明确保护本机编辑内容。
- 新增统一社区举报闭环：非本人公开用户、主题、楼层/回复、动态和动态评论均可选择八类原因并提交人工审核；游客保留登录回跳，`40107` 完成邮箱验证后恢复表单，重复待处理举报以 `40914` 明确收敛。
- 新增温油钱包与加油闭环：登录后按北京时间自动幂等签到，`/me/wallet` 精确展示余额、累计收款和 cursor 流水；用户、主题与动态详情支持稳定幂等加油、真实到账提示和服务端累计值刷新，六个 Wallet operationId 全部接入。
- 搜索补齐综合、动态和主题内正文入口：五个分类按需加载，动态/正文游标分页，主题详情可跨全部子贴搜索并定位；编辑器 @ 提及合并服务端全站用户候选，四个剩余搜索相关 operationId 形成可操作闭环。
- 新增动态完整闭环：五栏应用壳加入发现/关注信息流，公开详情与用户动态、受保护收藏、纯文本最多九图发布编辑删除、点赞收藏、两层评论、单图/收藏表情评论和 Foundation 原图查看全部接入 16 个 Moments operationId。
- 新增 capability 控制的收藏表情闭环：`/me/stickers` 支持相册上传导入、处理状态轮询、完整列表排序和移除；帖子/私聊图片可快速收藏，私信输入器与主题/帖子编辑器可直接使用收藏表情。
- 新增受保护的站内私聊闭环：会话/请求/归档列表、未读角标、新建与后续消息、历史/增量读取、接受/拒绝、归档、已读和十分钟撤回全部接入；陌生请求图片延迟加载，失败发送保留稳定幂等键，中心/会话/新私聊在 360/400/600dp 形成闭环。
- 新增公开 `/tags/:tagId` 标签主题页和受保护的主题标签工作台：标签可从首页/详情稳定跳转，楼主/协作者可搜索、创建、添加与移除最多五个标签；防抖竞态、同名 `40905` 恢复、`40107` 验证和 360/400/600dp 布局形成闭环。
- 主题管理新增受保护的完整子贴工作台：楼主/协作者可创建、读取详情、更新、删除和显式上下排序非默认子贴；默认子贴固定置顶且不可删除，创建幂等重试、更新版本冲突、邮箱验证恢复和 360/400/600dp 布局均形成闭环。
- 已发布私密主题新增完整邀请闭环：楼主确认旧链接失效后生成并复制 Web 兼容邀请，登录用户从受保护 `/join/:token` 预览、处理已加入状态并在邮箱验证后幂等加入；失效邀请不重复请求，邀请 token 从网络日志脱敏。
- 主题管理新增受保护的成员身份工作台：楼主/协作者可维护非楼主成员的玩家标记，只有楼主可二次确认任免协作者；非楼主玩家可在详情二次确认退出身份，成功后重读详情并失效对应订阅状态。
- 主题详情新增受保护的已有主题管理入口：楼主/协作者可更新标题、动态分区和状态，只有楼主可改可见范围并经二次确认删除；`40002` 冲突保留本机表单并由用户选择采用或覆盖最新版，未保存离页与 `40107` 验证恢复均形成闭环。
- 主题与帖子编辑器新增上下文 `@提及`：输入 `@` 后按主题查询关注用户和帖内玩家，服务端授权时提供 `@全体玩家`；候选支持防抖、竞态丢弃、空错重试与请求 ID，选择后写入 Markdown v2 原子节点。
- “我的”新增头像设置与移除：严格校验 JPG/PNG/WebP 头字节，复用预签名直传、确认和完成态轮询，按 `mediaId` 设置服务端头像；上传可取消，设置失败保留已上传媒体供单端点重试，移除要求二次确认并立即回到默认占位。
- 新增受保护的 `/me/security/delete-account` 账号注销闭环：明确已发布内容匿名保留与本地未发布草稿不可恢复风险，要求输入固定短语并二次确认；服务端注销成功后清除本机双 Token，本地清理失败只允许单向重试而不重复调用破坏性端点。
- 新增受保护的 `/me/security/verify-email` 邮箱验证闭环：读取当前账号事实、显式反枚举重发、60 秒/`Retry-After` 冷却、6 位验证码专用错误、验证后资料重读及原操作恢复均可操作；“我的”和创建主题失败态提供入口。
- 登录页新增 `/auth/forgot-password` 与 `/auth/reset-password` 账号恢复闭环：反枚举发送提示、60 秒/`Retry-After` 冷却、验证码专用错误、全终端撤销语义和原目标回跳均可操作。
- 新增 `/threads/:threadId/posts/:postId/replies` 独立楼中楼：公开阅读、最早/最新排序、回复者筛选、目标补齐、cursor 分页、下拉刷新和 360dp 布局形成闭环。
- 主题详情与楼中楼接入楼层/回复创建、作者编辑删除、管理者楼层删除及子贴正文 upsert；复用 Quill、图片、骰子和五槽位正文草稿，稳定幂等键与乐观锁冲突均要求明确确认。
- 编辑器工具栏接入后端用户级五槽位正文草稿：并发读取列表/用量、自动或指定空槽保存、恢复前刷新、版本覆盖、删除、满额提示和 360dp 面板均形成闭环；主题实体草稿继续保留独立入口。
- `/compose/thread` 接入真实主题创作页：Flutter Quill 工具栏、服务端分类/邮箱状态、按账号隔离的本地防丢快照、持久化创建幂等恢复，以及 `threadsCreate` 草稿后再用 `threadsSaveAggregate` 保存或发布的两阶段链路。
- 编辑器图片接入相册选择、10MB/类型校验、独立无鉴权预签名 PUT、确认与完成态轮询；只有 `COMPLETED` 的安全 HTTP(S) 原图会插入 Markdown，并提供上传取消、状态提示及可缩放全屏查看。
- Markdown ↔ Delta 编码器支持粗体、斜体、删除线、行内代码、安全链接、二三级标题、引用、列表和分隔线；不支持或冲突的格式会阻止保存，避免静默丢失。
- 建立自研 Markdown v2 扩展节点 ↔ Quill Delta Codec：提及、全体玩家、骰子、表情、图片与独占空段使用稳定内存协议；未知版本、非法节点和危险图片以只读兼容 embed 保留原文。
- “我的”新增修改密码与更换邮箱：严格映射三个后端账号安全端点，换绑验证码支持 60 秒/`Retry-After` 冷却；任一操作成功后按服务端全会话撤销语义清除本机 Token 并要求重新登录。
- “我的”新增登录终端管理：读取当前活跃 Web/移动端会话，当前终端只读，其他终端二次确认后按稳定 ID 撤销；界面不读取或展示已废弃的原始设备标识。
- 底部通知分支接入真实站内通知：支持六组类型筛选、游标分页、结构化 payload 文案、游客登录引导、下拉刷新、目标失效提示及主题/楼层/用户精确导航。
- 接入通知单条已读、全部已读、删除与独立未读数闭环；底栏角标每 30 秒和回前台刷新，写操作同步角标，乐观已读失败回滚并重新校准服务端未读事实。
- “我的”新增本人收藏管理页：接入游标分页、主题摘要导航与按收藏记录 ID 取消；写入成功后从当前筛选首屏权威刷新，不从收藏 ID 构造不透明游标，失败保留条目和请求 ID。
- 主题详情接入官方更新与玩家发言两类订阅：普通登录用户可按服务端记录 ID 可逆管理，玩家面板只展示已标记普通玩家，楼主/协作者沿用自动通知并隐藏控件。
- 主题详情接入点赞/取消点赞与收藏/取消收藏：游客点赞先登录并保留帖子目标，登录用户使用服务端权威计数和收藏记录 ID，失败保留原状态与请求 ID。
- 新增关注、粉丝与黑名单移动端列表：公开用户统计进入指定用户列表，“我的”进入本人列表；黑名单支持失败保留、请求 ID 与原地取消拉黑。
- 公开用户主页新增关注/取消关注与拉黑/解除拉黑闭环：认证身份确认后显示，关系写入串行化，拉黑二次确认，失败保留原状态与请求 ID。
- “我的”分支接入本人真实资料与编辑闭环：展示邮箱验证、成长进度和统计，支持独立改名、简介与三项公开范围更新，并保留失败重试和安全退出。
- 公开用户主页新增移动端内容分区：创建主题始终可见，参与主题、最近回复与收藏严格随公开隐私字段显示，四类数据独立惰性加载并支持游标分页、局部错误与目标导航。
- 实现全站搜索移动端闭环：主题、用户、正文三个惰性 Tab，正文游标分页、竞态保护、安全 Markdown 摘要及完整空错重试状态。
- 新增只读公开用户主页，展示公开资料、温油统计与关系状态；搜索结果和 Markdown 用户站内链接统一进入稳定用户路径。
- 新增 `/threads/:threadId?post=:postId` 正文结果定位，自动解析所属子贴并为主楼层或楼中楼补齐、强调目标上下文。
- 锁定并消费 `wenyousite-foundation` v1.1.0 Flutter 包，接入中央语义 Token、移动 profile 与三角色自托管字体。
- 实现 0.3 公开阅读切片：首页主题卡片进入稳定主题详情，详情展示元数据、子贴 Markdown v2 正文、升序楼层分页与服务端内嵌回复。
- 新增安全 Markdown 阅读组件，支持外部链接/图片协议约束和服务端骰子结果展示，并为未知结果提供可复制的降级文本。
- 实现首页推荐主题流首个闭环：公网列表、服务端动态分类、状态/排序筛选、下拉刷新、游标分页及紧凑主题卡片。
- 建立 Flutter Android 工程、固定 OpenAPI 生成链、契约快照、中文模块文档体系与文档门禁。
- 建立初始主题适配、应用环境、网络/会话、本地持久化与 Markdown v2 基线。
- 确定 Flutter Quill 仅作内存编辑模型，并建立自研 Markdown v2 ↔ Delta Codec 边界与测试规范。
- 同步 Markdown v2 扩展节点往返语料和 mobile push v1 Schema/黄金样例，为后续编辑器与推送切片固定跨端边界。
- 实现账号/密码登录、mobile 终端声明、双 Token 安全落库，以及创建主题入口的登录回跳。
- 完成会话刷新与退出闭环：并发刷新锁、单次请求重放、失效原因导航、服务端会话撤销及明确确认的本机退出后备。
- 实现邮箱验证码两步注册、重发冷却、专用错误状态、mobile 双 Token 自动登录及原目标回跳。
- 建立语义主题适配层，以及面板、区块标题、状态提示、异步主按钮、空状态等共享组件。

### Changed

- 收藏列表、主题互动、主题订阅、用户关系与关系列表五类仓储统一收束为 `social/application/*_ports.dart`，由 `main.dart` 组合根绑定 API 适配器；social 控制器不再导入具体 data 实现，服务端计数/记录 ID、分类局部失败、乐观关系写入与失败恢复语义保持不变。
- 私聊、动态与收藏表情仓储，以及动态本机草稿存储，统一收束为各自 `application/*_ports.dart`，由 `main.dart` 组合根绑定 API/SharedPreferences 适配器；控制器与动态编辑页不再导入具体 data 实现，会话对账、动态幂等写入、草稿恢复和表情处理轮询语义保持不变。
- 五槽位正文草稿、主题创作/提及/本地快照和帖子读写仓储统一收束为各自 `application/*_ports.dart`，由 `main.dart` 组合根绑定 API/Drift 适配器；相关控制器不再导入具体 data 实现，幂等创建、乐观锁冲突、远端草稿切换和本地防丢语义保持不变。
- 首页、通知、搜索、举报、治理申诉与钱包六个独立仓储统一收束为各自 `application/*_ports.dart`，由 `main.dart` 组合根绑定 data 适配器；控制器不再导入具体 data 仓储，分页、乐观恢复、专用凭据、幂等请求和 fail-closed 映射保持不变。
- 账号注销、凭据安全和登录终端仓储收束为 `settings/application` 端口，由 `main.dart` 组合根绑定 API 适配器；settings 控制器不再导入具体 data 仓储，远端注销后的单向本地清理、全端会话撤销和终端列表恢复语义保持不变。
- 公开资料、本人资料和头像写入仓储收束为 `users/application` 公共端口，由 `main.dart` 组合根绑定 API 适配器；users 控制器及私聊目标解析不再导入 users data，资料分页、头像单端点重试和新私聊目标校验语义不变。
- 登录/注册、邮箱验证和密码恢复仓储收束为 `auth/application` 端口，由 `main.dart` 组合根绑定生成客户端适配器；四个认证控制器不再导入具体 data 仓储，既有双 Token、反枚举、冷却和错误恢复语义保持不变。
- 应用壳把元信息读取、移动端更新和推荐更新忽略记录收束为 `application` 端口，由 `main.dart` 组合根绑定 data 实现；启动与更新控制器不再直接导入 Dio 或具体仓储，同时保持既有兼容门禁、下载校验和忽略语义。
- 头像图片的格式策略、相册端口和上传生命周期迁入 `media/application`：`AvatarController` 不再导入 Dio 或 media data 实现，只映射共享进度/取消并负责设置、移除及已完成 `mediaId` 的单端点重试。
- 表情包管理页的相册导入迁入 `media/application` 共享上传任务：页面不再持有 Dio 取消令牌或直接读取 media data provider，上传失败可复用同一文件，取消立即恢复管理操作，只有完成的 `mediaId` 才进入稳定幂等导入端点。
- 私聊图片选择与上传迁入 `media/application` 共享任务控制器：输入器只消费上传完成的 `mediaId`，失败可复用同一文件重试，上传中取消立即解除等待并保留正文、选区与焦点；私聊 presentation 不再导入 media data 或 Dio。
- 主题与帖子编辑器的图片上传迁入 `media/application` 共享任务控制器：页面按实例隔离选择、阶段进度、取消、失败与同文件重试，只在完成后插入固定 `[图片]` 节点；Dio 取消令牌与具体仓储留在 data 层，并由 `WenyouApp` 组合根绑定端口。
- 开发版本进入 `0.3.0-dev.69+75`；Android 11+ 输入页停止跟随 IME 动画的每帧 inset 缩放根布局，由原生窗口动画边界将键盘最终高度一次传入 Flutter，输入框、富文本编辑器和 Dock 在首帧直接到达键盘上方，开启与收起都不再逐帧位移；Android 8–10 保留系统兼容避让，防止旧系统无窗口动画边界时遮住输入区。
- 开发版本进入 `0.3.0-dev.68+74`；三类详情流程与回复 Dock 改为默认无动画，富文本列表保留已解析正文并提前构建以修复快速滑动白屏，私聊首次打开直接定位最新消息，本人与公开用户主页统一为全宽移动资料头和等宽内容分区。后端契约仍为 `4.7.0-dev.20260811.1` / `acd20b07c619f85a17d42dd40cccae03122394be`，Foundation 仍锁定最新发布 `v1.3.1` / `7cf71327aaeee4f5e6baae069335c8a606f6a911`。真机安装、操作与体感验收改由项目负责人手动执行，开发代理仅交付 Debug APK 与手测清单，未得当次明确授权不操作 ADB。
- 本人中心与公开用户主页重构为统一全宽移动资料头：头像、身份、简介和加入时间形成单一信息层级，关注/粉丝/收到加油使用等宽统计列，主要操作占满分配宽度；公开内容改为等宽页签与单列全宽卡片，不再由文本长度决定面板宽度。
- 修复快速滚动详情正文先白屏再自上而下出现：主题楼层、独立回复与动态评论扩大视口缓存，Markdown 楼层保留已解析 Widget 状态并提前触发分页；私聊时间线改为最新消息位于反向列表起点，首次进入和重新打开直接停在最后一条且不恢复旧历史位置。
- 主题详情、独立楼中楼与动态详情改为固定顶栏和固定悬浮发表入口，三类详情统一无路由、分页正文、顶栏或悬浮按钮入场动画；私聊和帖子回复 dock 的键盘避让、帖子格式托盘与编辑器底部面板均即时切换。动态评论移除可拖拽半屏双层输入区，普通评论与定向回复统一为唯一的私聊式单体 dock，并保留附件、字数、发送状态和未发送离开保护。
- 移动组件调整为性能优先的默认无动画策略：移除状态提示、异步按钮、编辑器托盘与网络图片的交叉淡入；全站缓存图片即时切换，列表缩略图按显示尺寸解码。首页、动态瀑布流与私聊时间线释放离屏 keep-alive；富文本详情改为按视口懒构建并保留已经解析的正文状态，兼顾长列表内存与快速滚动连续性。
- 私聊同步 Web 的乐观发送与稳定对账语义：消息点击发送即进入时间线，失败随气泡原位重试且不锁输入；活跃会话采用前台追赶轮询，时间线保持历史锚点并在离底时显示新消息浮标。会话气泡按连续发送者收束，复制、撤回和收藏移入长按菜单；输入区改为单体 dock，会话头部与列表删除恒定状态、箭头和重复说明。
- 开发版本进入 `0.3.0-dev.67+73`；全部内容创作面统一为键盘上方固定 dock：主题使用剩余正文画布，楼层/回复与动态评论使用可扩展半屏，私信保持会话内联；格式层级与“更多”留在编辑器内部，骰子在阅读与编辑态均按正文基线行内排版。主题楼层取消零回复按钮，点击普通楼层区域直接发起回复；主题列表保留卡片与整宽封面并补充作者头像。同步 Web `7ba1b83` 删除重复教学文案，但保留错误、风险、权限、状态与不可逆确认。
- 开发版本进入 `0.3.0-dev.66+72`；按首页发现的独立内容单元语义恢复白色细边框圆角卡片，唯一安全首图在卡片内以整宽 16:9 展示，无图不预留图片区，320/360/400/600dp 与无图/有图视觉基线固定布局。契约同步新增生产运行时门禁：除重新生成 Dart 客户端并要求零差异外，还会比对公网 `/meta` 的契约版本与后端提交，并冒烟校验 `/threads` 单封面响应；后端部署与移动端正式发布状态不再混为一谈。
- 开发版本进入 `0.3.0-dev.65+71`；独立楼中楼首屏移除正文区重复的主题名与子贴名，顶栏改为真实“主题 / 子贴 · 楼层”两级语境，让原楼层正文约提前 60dp 出现；回复数后不再常驻排序与作者筛选两个控件，合并为一个弱化的讨论设置入口，按需底部面板仍完整提供回复顺序与只看回复者。320/360/400/600dp、2 倍系统字号、设置实效与更新后的 360dp 视觉基线固定文字优先且能力不丢失。
- 开发版本进入 `0.3.0-dev.64+70`；楼主与协作者阅读主题时，当前子贴正文前不再永久多出 48dp 编辑图标和额外间距，管理者首屏与普通读者共享同一正文优先排版；“编辑/添加当前子贴正文”迁入已有顶栏“更多主题操作”，显示当前子贴标题，空正文使用明确添加文案，360/400/600dp 与独立管理者视觉基线固定能力可达且阅读区无常驻管理控件。
- 开发版本进入 `0.3.0-dev.63+69`；主题、子贴正文、楼层和独立讨论中的普通 Markdown 图片移除永久覆盖内容的“添加到表情收藏”按钮，整张图片只承担查看原图语义；共享黑底原图页统一承载系统返回、双击缩放/复位、捏合、平移和未放大下滑关闭，登录且能力开启时再从顶栏“图片操作”按需收藏，进行中锁定避免重复提交。正文内规范 sticker 仍保持 96dp 原子展示，不伪装成普通插图。
- 开发版本进入 `0.3.0-dev.62+68`；主题与帖子编辑器的 `@提及` 候选不再作为整块内容插入并压缩正文，改为靠近软键盘、最多 200dp 高且内部滚动的临时浮层；320～400dp 键盘写作会同时避开 48dp 格式工具入口，出现、关闭或选择候选均不改变正文画布尺寸、Quill 焦点和当前选区，并新增 2× 字号与视觉基线。
- 开发版本进入 `0.3.0-dev.61+67`；首页与公开标签页共享主题条目从逐张圆角面板改为分隔线连续文章索引，分类、状态、置顶和标签合并为单行弱化语境，标题与摘要优先，统计去除装饰图标；无图主题在 360dp 收敛至 190dp 内，有图主题使用 104×88dp 右侧缩略图而不再铺满 16:9 舞台，并新增独立讨论阅读视觉基线。
- 开发版本进入 `0.3.0-dev.60+66`；主题详情排头移除完整圆角卡片、状态胶囊和逐项统计图标，改为扁平题头、单行弱化语境与紧凑元信息，题头在 360/400/600dp 均收敛到 190dp 内；最多五个标签保持单行横向浏览，首屏可同时看到主题标题、子贴正文和第一楼，并新增三档首屏视觉基线。
- 开发版本进入 `0.3.0-dev.59+65`；主题详情默认不再把楼中楼预览正文插进接力楼层之间，每层只保留 48dp 的弱化回复数入口，完整讨论集中到独立楼中楼页；具体回复的 `post` 深链直接打开并定位独立讨论，返回主题后不循环跳转，并新增双楼层连续阅读视觉基线。
- 开发版本进入 `0.3.0-dev.58+64`；主题与帖子 Quill 编辑器统一为 17sp、1.75 行高的长文创作节奏，窄屏正文聚焦且软键盘可见时不再让五枚格式按钮常驻扣减画布，改用不参与正文布局的 48dp 浮动“格式工具”入口，原地展开后保留全部格式能力、焦点与选区，并新增 320/360/400dp 键盘回归和 360dp 长文/键盘视觉基线。
- 开发版本进入 `0.3.0-dev.57+63`；主题详情、独立楼中楼与动态详情改为滚动感知的专注阅读模式：向下阅读同时收起 56dp 顶栏与悬浮发表入口，向上轻滑立即恢复，无障碍导航保持入口常显；新增 360dp 主题长文与楼中楼连续阅读视觉基线。
- 开发版本进入 `0.3.0-dev.56+62`；主题详情、独立楼中楼与动态详情移除永久压缩正文视口的整宽发表底栏，统一为按内容收缩、48dp 命中区的柔和悬浮入口，并在列表末尾补齐安全留白与 360dp 视觉基线。
- 开发版本进入 `0.3.0-dev.55+61`；“写主题”把发布收进 48dp 紧凑顶栏，完整主题草稿的保存与打开合并为单一按需入口，移除页面底部两条整宽动作；正文画布按窗口高度扩展，360×800dp 从 340dp 提升到 440dp，并补齐 360/400/600dp 回归。
- 开发版本进入 `0.3.0-dev.54+60`；主题详情顶栏只保留搜索与单一更多入口，管理、加油和举报改为按需底部操作面板，减少低频按钮对标题和阅读画布的挤占；长篇 Markdown 固定克制的标题比例、17sp/1.8 正文、轻引用与 1px 分隔线，内嵌楼中楼恢复 16sp/1.75 阅读节奏，并新增 360dp 长文视觉黄金基线。
- 开发版本进入 `0.3.0-dev.53+59`；楼层、回复与子贴正文编辑器改为全高纯正文画布，移除说明行、内嵌圆角卡片和底部整宽提交带，将关闭、目标与提交收进 48dp 紧凑顶栏；打开自动聚焦，编辑或恢复正文时光标落在文末。独立楼中楼把回复总数、排序与作者筛选合并为同一行，并新增 360dp 视觉黄金基线。
- 开发版本进入 `0.3.0-dev.52+58`；首页主题卡片移除重复大头像并改为标题、摘要优先，主题正文和楼层收敛为分隔线连续阅读流，零回复楼层提供可见回复入口；“写主题”首屏释放给标题和至少 340dp 正文画布，服务端草稿收进顶栏，发布设置默认折叠，并新增可读中文字体的 360dp 视觉基线。
- 开发版本进入 `0.3.0-dev.51+57`；同步后端 `4.7.0-dev.20260811.1` 与 Foundation `1.2.1`，接入受处罚账号的专用凭据申诉闭环和站内传送门 v1；主题详情排头收敛为单面板同排工具栏，楼中楼等级徽标恢复内容宽度。
- 开发版本进入 `0.3.0-dev.50+56`；移动端对齐 Web 收藏夹实例化样式与交互层级，接入 `bookmarksFindFolders`、`bookmarksCreateFolder`、`bookmarksMove`，移动/取消后同步刷新当前筛选和分类计数，失效 cursor 从首屏恢复；详情快捷收藏继续进入默认收藏夹。
- 开发版本进入 `0.3.0-dev.49+55`；首页与搜索主题卡片收紧为单封面 16:9 展示，兼容超契约多图响应时也只采用首图。登录、退出、账号注销或会话失效后立即权威刷新首页发现列表，避免长驻内存仍展示已注销楼主的旧帖。
- 同步后端契约 `4.5.2-dev.20260811.1`：首页与标签发现排除已注销楼主的主题，显式搜索仍保留公开历史内容；首页和主题搜索封面收紧为默认主贴的唯一首图。接口、operationId 与字段结构保持兼容，并已重新生成 Dart 客户端。
- 开发版本进入 `0.3.0-dev.48+54`；主题详情的子贴入口改为显示当前标题和楼层数的纵向目录面板，明确禁止横向滑动目录，左右快捷按钮按完整目录首尾循环。主题详情系统返回在有来源栈时正常返回、直接进入无来源栈时回首页，不再退出应用。首页分类、排序和状态统一为同样式的紧凑下拉菜单，状态筛选不再打开底部抽屉。“我的表情”等主入口文案统一为“表情包”，并修复 capability 内层覆盖下打开页面触发 Riverpod 依赖断言的红屏。
- 开发版本进入 `0.3.0-dev.47+53`；主题列表和主题详情的标签收敛为无图标、无底色、无边框的行内 `#标签` 文本链接，缩短标签区留白并保留 48dp 触控目标；标签管理工作台继续使用带删除能力的编辑 Chip。
- 开发版本进入 `0.3.0-dev.46+52`；主题详情排头改为标题优先的紧凑信息层级，移除统计墙、独立子贴说明卡和楼层说明标题；当前子贴收进排头卡片，可展开完整目录并用左右按钮快速切换。主题详情与独立讨论取消页面入场和目标定位动画，骰子结果只更新对应内联节点，不再重建整段 Markdown，消除正文由上到下重新浮现。统一纵向内容宽度约束，首页、详情、通知、收藏、关系、标签、终端、表情与个人中心等列表的卡片不再随文字长短收缩。

- 开发版本进入 `0.3.0-dev.45+51`；主题详情与独立讨论中的楼层/回复统一改为长按操作菜单，移除卡片底部常驻举报/编辑/删除，等级徽标与相对时间对齐 Web 且弱化楼层元信息；底栏搜索替换为中央粉色发布动作，首页与动态顶栏提供搜索入口，并移除回复卡片弹出动画。

- 开发版本进入 `0.3.0-dev.44+50`；修复骰子自定义标签被 Markdown 当作普通文本展示的问题，改为原生内联语法解析，并在骰子结果异步到达时立即刷新无图标结果标签。

- 开发版本进入 `0.3.0-dev.43+49`；回复卡片改为长按操作菜单，支持复制正文、复制楼层链接、回复、编辑、删除和举报；站内链接在移动端自动进入应用路由，骰子结果与 Web 一致显示为无图标内联标签。

- 开发版本进入 `0.3.0-dev.42+48`；修复私信能力开启时 Riverpod 未声明 `directUnreadControllerProvider` 依赖，避免应用启动触发 provider override 断言。

- “我的”从长表单重构为本人摘要、成长统计和内容捷径，资料编辑与账号设置拆分到 `/me/edit`、`/me/settings`；五个主分支统一使用“发布”类型面板，底栏“消息”合并通知与私信独立未读事实。
- 首页与讨论筛选收敛为分类/排序常驻、低频条件底部面板；搜索改为动态、主题帖、楼层内容、用户四个全宽纯文字页签，不再暴露综合类型或横向滚动。
- 动态详情相册在 360dp 释放到 336dp 页面宽度，标题/正文保留独立留白；楼层、回复、评论、通知和会话改为分隔线连续列表，仅为未读或精确目标保留状态底色。
- 移除仍展示粉色整页背景、四栏旧导航和公网开发环境提示的三张历史设备截图，新增 Foundation v1.1.0 现行视觉基线文档，手工截图不再作为长期设计事实。
- 开发版本进入 `0.3.0-dev.41+47`；修复移动端刷新会话漏传 `X-Client-Platform: mobile` 导致 access token 过期后误丢登录态的问题，网络或服务暂时不可用时保留本地会话供后续重试。
- 开发版本进入 `0.3.0-dev.40+46`；动态详情多图由网格收敛为移动端单图横滑轮播，标题、轮播和正文顺序对齐 Web 样例，舞台按封面比例固定且图片完整 `contain`，切换时不推动正文，并保留当前位置与对应原图查看入口。
- 开发版本进入 `0.3.0-dev.39+45`；Android 发布流程从每次临时导出密钥和手工命令收敛为仓库内可审计脚本与桌面入口，DPAPI 密文、SSH 私钥、keystore 和本机路径配置继续排除在 Git 之外。
- 开发版本进入 `0.3.0-dev.38+44`；动态发现、关注、收藏与用户信息流改为 12dp 间距的双列瀑布流，列表卡片收敛为按服务端尺寸预留的封面、两行标题、作者和点赞，首屏骨架、自动/手动分页、空错重试、TalkBack 标签及 48dp 触控目标保持完整。
- 开发版本进入 `0.3.0-dev.37+43`；Android 在线更新在下载前后核对 RainS3 的包类型、长度、应用 ID、版本和 SHA-256，损坏缓存自动清除并以 `.part` 原子落盘，原生安装桥再校验包名、目标构建和当前应用签名；授权返回复用本次已验证 APK，更新页展示检查、下载、校验与安装阶段。同步移动 V1 黄金旅程后，PUT/DELETE 瞬时失败也按幂等语义自动重试，PATCH、普通 POST、4xx 与 429 仍不自动重放。
- 同步后端契约 `4.5.0-dev.20260809.1`：固定 196 个 operationId 的移动分类与 V1 黄金旅程，公开用户状态、默认收藏夹分类和站务错误码均为向后兼容新增；本轮在线更新继续消费既有 `/meta.mobileCompatibility`，新收藏夹分类与用户申诉明确留作独立切片。
- 开发版本进入 `0.3.0-dev.36+42`；携带稳定 `clientRequestId` 的创建调用统一声明 `ApiRequestPolicy.idempotentCreate` 并由真实 Dio 重试测试固定请求 ID，feature 业务错误文案退出 core；新增 domain 依赖、跨 feature 边、路由字面量、版本一致性和未使用依赖门禁，并移除四个未使用直接依赖。
- app 组合层接管 capability 和跨 feature 缓存失效，主题卡片读模型归属 threads；路由位置与访问级别改为纯 Dart 定义，首页/标签复用请求世代和分页合并工具，主题详情、私聊目标与动态编辑器协调文件完成接口不变的职责拆分。
- 新增登录回跳策略、真实幂等重试、跨 feature 缓存失效、TalkBack 语义与 Foundation 核心视觉黄金图回归；OpenAPI 适用范围保持 138/138。
- 开发版本进入 `0.3.0-dev.35+41`；原始契约覆盖由 137/167 提升为 138/167，移动端适用范围达到 138/138；其余 29 个管理/运维/FCM/废弃端点进入逐 operationId 审核的排除门禁，服务端主题草稿与五槽位正文草稿继续保持独立资源和明确恢复语义。
- 开发版本进入 `0.3.0-dev.34+40`；移动端 API 覆盖由 136/167 提升为 137/167，用户举报与管理员审核职责明确分离，非幂等举报不会自动重放。
- 开发版本进入 `0.3.0-dev.33+39`；移动端 API 覆盖由 130/167 提升为 136/167，全部温油金额保留十进制整数字符串，动态累计加油不再经过浮点数映射；余额/流水局部失败、失效 cursor、三类业务错误和不明确加油均有安全恢复路径。
- 开发版本进入 `0.3.0-dev.32+38`；移动端 API 覆盖由 126/167 提升为 130/167，综合/动态/主题内搜索的短词、竞态、失效 cursor、跨主题响应与不安全动态 DTO，以及全站提及候选合并均有安全恢复路径。
- 开发版本进入 `0.3.0-dev.31+37`；新增 OpenAPI operationId 覆盖审计工具，移动端覆盖由 110/167 提升为 126/167，动态 cursor 失效、编辑冲突、`40415`、不明确创建/评论与图片/表情互斥均有安全恢复路径。
- 开发版本进入 `0.3.0-dev.30+36`；Stickers 的 7 个 operationId 完整接入，收藏夹版本冲突、200 个容量上限、异步处理失败和不明确导入均保留安全恢复路径，陌生消息请求图片在明确查看前不暴露收藏入口。
- 开发版本进入 `0.3.0-dev.29+35`；Direct Messages 的 11 个 operationId 完整接入，`meta.capabilities.directMessages` 控制通知页与用户主页入口，前台轮询只保留进程内私聊事实且不记录完整正文。
- 开发版本进入 `0.3.0-dev.28+34`；`tagsSearch`、`tagsGetById`、`tagsCreate`、`threadTagsFindAll`、`threadTagsAdd` 与 `threadTagsRemove` 完整接入，公开主题流复用 `threadsFindAll(tagId)` 精确聚合。
- 开发版本进入 `0.3.0-dev.27+33`；`subthreadsFindAll`、`subthreadsFindById`、`subthreadsCreate`、`subthreadsUpdate`、`subthreadsRemove` 与 `subthreadsReorder` 完整接入，子贴正文继续复用 posts 模块的独立乐观锁编辑闭环。
- 开发版本进入 `0.3.0-dev.26+32`；`threadsCreateInviteLink`、`threadsPreviewInviteLink` 与 `threadsJoinByInviteLink` 完整接入，移动端不持久化邀请凭据，外部分享继续由 Web 链接承接。
- 开发版本进入 `0.3.0-dev.25+31`；`threadMembersUpdateMember` 与 `threadMembersExitMember` 完整接入，已废弃的手动加入端点不在移动端暴露，成员身份继续由真实业务行为建立。
- 开发版本进入 `0.3.0-dev.24+30`；`threadsUpdate` 与 `threadsRemove` 完整接入，移动端严格区分协作者和楼主字段权限，更新只发送变化字段与当前版本，删除必须得到服务端明确确认。
- 开发版本进入 `0.3.0-dev.23+29`；`usersMentionCandidates` 完整接入，Foundation v1.1.0 固定工具栏保持不变，移动端通过正文内 `@` 触发候选，新主题未形成服务端草稿时明确要求先保存以取得真实 `threadId`。
- 开发版本进入 `0.3.0-dev.22+28`；`usersSetAvatar` 与 `usersRemoveAvatar` 完整接入，头像只消费服务端明确返回的安全 HTTP(S) 地址，旧头像缓存主动失效且重启后重新读取本人事实。
- 开发版本进入 `0.3.0-dev.21+27`；`usersDeleteMe` 完整接入，后端匿名化账号并撤销全部 refresh 会话后，移动端不会以失败的本地清理结果重放远端注销。
- 开发版本进入 `0.3.0-dev.20+26`；`authVerifyEmail` 与 `authResendVerification` 完整接入，`40107` 保留只读会话和编辑草稿，验证成功不换 Token，只在重读本人资料确认后恢复发布资格。
- 开发版本进入 `0.3.0-dev.19+25`；`authForgotPassword` 与 `authResetPassword` 完整接入，失效会话也可进入公开找回流程，成功后不建立伪会话而是要求使用新密码登录。
- 开发版本进入 `0.3.0-dev.18+24`；`postsFindReplies`、`postsCreate`、`postsUpdate`、`postsUpsertBody`、`postsRemove` 与既有楼层读取全部闭环，`40302/40303`、`40007`、`40002` 和不明确创建结果有稳定恢复状态。
- 开发版本进入 `0.3.0-dev.17+23`；`draftsFindAll`、`draftsSlotUsage`、`draftsFindById`、`draftsCreate`、`draftsUpdate`、`draftsRemove` 全部接入，`40002` 冲突保留当前正文并要求基于最新版二次确认。
- 开发版本进入 `0.3.0-dev.16+22`；主题创建、服务端草稿、聚合发布、媒体上传和本地快照形成首个可真机操作的创作闭环，后端五槽位正文草稿仍作为后续独立切片。
- 开发版本进入 `0.3.0-dev.15+21`；两套后端 Markdown 黄金语料现同时验证规范化、可见性、扩展节点字段和 Delta 往返幂等，损坏 embed 不再允许静默序列化。
- 开发版本进入 `0.3.0-dev.14+20`；`/me/security/password` 与 `/me/security/email` 加入登录守卫，账号安全写入使用独立 autoDispose 状态并保留失败请求 ID。
- 开发版本进入 `0.3.0-dev.13+19`；`/me/security/sessions` 加入登录守卫与安全回跳，终端读取、失败恢复和撤销写入使用独立 autoDispose 状态。
- 开发版本进入 `0.3.0-dev.12+18`；通知占位页替换为 API 驱动消息中心，未知类型与未知目标保持可读且不猜测导航。
- 开发版本进入 `0.3.0-dev.11+17`；`/me/bookmarks` 加入登录守卫与安全回跳，社交模块的主题收藏读取/创建/删除端点全部闭环。
- 开发版本进入 `0.3.0-dev.10+16`；订阅列表与帖内成员候选并发读取，两类写入共享串行状态、失败恢复与请求 ID。
- 开发版本进入 `0.3.0-dev.9+15`；主题详情从只读统计升级为可恢复互动，喜欢计数与操作状态共享同一会话内事实。
- 开发版本进入 `0.3.0-dev.8+14`；五个关系列表读取端点完成映射，私有列表路由加入登录回跳与竞态保护。
- 开发版本进入 `0.3.0-dev.7+13`；社交模块进入 `in_progress`，公开用户关系操作使用独立会话内状态并同步粉丝计数。
- 开发版本进入 `0.3.0-dev.6+12`；“我的”从会话占位页升级为 Foundation 风格的本人资料总览与服务端权威编辑表单。
- 开发版本进入 `0.3.0-dev.5+11`；公开用户页从资料摘要扩展为真实内容浏览，移动端用横向分段切换替代 Web 端纵向堆叠长列表。
- 开发版本进入 `0.3.0-dev.4+10`；搜索与用户模块进入 `in_progress`，动态搜索、用户内容 Tab 和关系写操作按对应模块闭环后续接入。
- 开发版本进入 `0.3.0-dev.3+9`；全局主题迁移到 Foundation 纯白画布、柔粉主色、深紫灰正文、中央状态色和可见焦点，业务流程与 API 状态机不变。
- 删除移动端平行审美指导，模块文档直接引用锁定版本的 Foundation Flutter profile。
- 开发版本进入 `0.3.0-dev.2+8`，主题与楼层模块进入 `in_progress`；本轮保持只读，独立楼中楼页、精确定位、回复及其他写操作后置。
- 首页刷新入口收敛为下拉刷新和错误态重试，主题卡片承担进入公开详情的明确导航职责。
- 详情阅读正文规范调整为约 17sp、1.75–1.85 行高，内嵌回复使用 16sp、约 1.75 行高，并补充主题详情与两层回复的面板规范。
- 开发版本进入 `0.3.0-dev.1+7`，首页从应用壳占位页切换为真实公开内容流。
- 同步后端契约 `4.4.0-dev.20260809.1`：主题分类改为服务端动态配置，主题列表增加封面与温油数据，并接入动态、钱包、成长体系及分平台构建兼容类型。
- 启动兼容门禁切换到后端 `4.x` 主版本，并继续拒绝未审查的未来主版本。
- 第一阶段开发改为相关本地检查与真机冒烟优先；GitHub Actions 暂停自动触发，仅保留阶段验收时手动执行。
- 修复生成 API 客户端与 Dio 基地址重复拼接 `/api/v1`，导致真机启动请求 404 的问题。
- OpenAPI Dart 客户端使用稳定的 `built_value` 序列化，未知枚举安全降级。
- 刷新和退出统一通过生成客户端发送稳定请求 ID；重放再次过期或收到 `40103`～`40106` 时清除本地会话并进入登录页。
- `ApiFailure` 读取 `Retry-After` 并为验证码过期/错误、邮箱已注册和用户名占用提供稳定移动端提示。
- 应用壳、启动状态、登录和注册统一复用共享面板、响应式页边距和至少 48dp 触控区域。

### Contract

- 后端移动接入指南恢复为 `0fb9d35` 的只读同步快照；私有构建与发布运维继续由移动端本地 `mobile-release-operations.md` 维护，不再从后端已移除的路径复制。
- 视觉与跨端体验契约锁定 Foundation `1.3.1`（`7cf71327aaeee4f5e6baae069335c8a606f6a911`）。
- 后端契约 `4.7.0-dev.20260811.1`，后端提交 `acd20b07c619f85a17d42dd40cccae03122394be`，Markdown v2、站内传送门 v1；本次后端未改变 OpenAPI，移动端同步新增 19 组编辑器 Markdown 往返黄金语料，并继续消费移动 V1 黄金旅程、operationId 分类与动态分类 fixture。
