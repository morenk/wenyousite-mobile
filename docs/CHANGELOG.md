# 移动端变更记录

## Unreleased

### Added

- 已发布私密主题新增完整邀请闭环：楼主确认旧链接失效后生成并复制 Web 兼容邀请，登录用户从受保护 `/join/:token` 预览、处理已加入状态并在邮箱验证后幂等加入；失效邀请不重复请求，邀请 token 从网络日志脱敏。
- 主题管理新增受保护的成员身份工作台：楼主/协作者可维护非楼主成员的玩家标记，只有楼主可二次确认任免协作者；非楼主玩家可在详情二次确认退出身份，成功后重读详情并失效对应订阅状态。
- 主题详情新增受保护的已有主题管理入口：楼主/协作者可更新标题、动态分区和状态，只有楼主可改可见范围并经二次确认删除；`40002` 冲突保留本机表单并由用户选择采用或覆盖最新版，未保存离页与 `40107` 验证恢复均形成闭环。
- 主题与帖子编辑器新增上下文 `@提及`：输入 `@` 后按主题查询关注用户和帖内玩家，服务端授权时提供 `@全体玩家`；候选支持防抖、竞态丢弃、空错重试与请求 ID，选择后写入 Markdown v2 原子节点。
- “我的”新增头像设置与移除：严格校验 JPG/PNG/WebP 头字节，复用预签名直传、确认和完成态轮询，按 `mediaId` 设置服务端头像；上传可取消，设置失败保留已上传媒体供单端点重试，移除要求二次确认并立即回到默认占位。
- 新增受保护的 `/me/security/delete-account` 账号注销闭环：明确已发布内容匿名保留与本地未发布草稿不可恢复风险，要求输入固定短语并二次确认；服务端注销成功后清除本机双 Token，本地清理失败只允许单向重试而不重复调用破坏性端点。
- 新增受保护的 `/me/security/verify-email` 邮箱验证闭环：读取当前账号事实、显式反枚举重发、60 秒/`Retry-After` 冷却、6 位验证码专用错误、验证后资料重读及原操作恢复均可操作；“我的”和创建主题失败态提供入口。
- 登录页新增 `/auth/forgot-password` 与 `/auth/reset-password` 账号恢复闭环：反枚举发送提示、60 秒/`Retry-After` 冷却、验证码专用错误、全终端撤销语义和原目标回跳均可操作。
- 新增 `/threads/:threadId/posts/:postId/replies` 独立楼中楼：公开阅读、最早/最新排序、回复者筛选、目标补齐、cursor 分页、下拉刷新和 360dp 布局形成闭环。
- 主题详情与楼中楼接入楼层/回复创建、作者编辑删除、管理者楼层删除及子贴正文 upsert；复用 Quill、图片、骰子和五槽位正文草稿，稳定幂等键与乐观锁冲突均要求明确确认。
- 编辑器工具栏接入后端用户级五槽位正文草稿：并发读取列表/用量、自动或指定空槽保存、恢复前刷新、版本覆盖、删除、满额提示和 360dp 面板均形成闭环；主题实体草稿继续保留独立入口。
- `/compose/thread` 接入真实主题创作页：Flutter Quill 工具栏、服务端分类/邮箱状态、按账号隔离的本地防丢快照、持久化创建幂等恢复，以及 `threadsCreate` 草稿后再用 `threadsSaveAggregate` 保存或发布的两阶段链路。
- 编辑器图片接入相册选择、10MB/类型校验、独立无鉴权预签名 PUT、确认与完成态轮询；只有 `COMPLETED` 的安全 HTTP(S) 原图会插入 Markdown，并提供上传取消、状态提示及可缩放全屏查看。
- Markdown ↔ Delta 编码器支持粗体、斜体、删除线、行内代码、安全链接、二三级标题、引用、列表和分隔线；不支持或冲突的格式会阻止保存，避免静默丢失。
- 建立自研 Markdown v2 扩展节点 ↔ Quill Delta Codec：提及、全体玩家、骰子、表情、图片与独占空段使用稳定内存协议；未知版本、非法节点和危险图片以只读兼容 embed 保留原文。
- “我的”新增修改密码与更换邮箱：严格映射三个后端账号安全端点，换绑验证码支持 60 秒/`Retry-After` 冷却；任一操作成功后按服务端全会话撤销语义清除本机 Token 并要求重新登录。
- “我的”新增登录终端管理：读取当前活跃 Web/移动端会话，当前终端只读，其他终端二次确认后按稳定 ID 撤销；界面不读取或展示已废弃的原始设备标识。
- 底部通知分支接入真实站内通知：支持六组类型筛选、游标分页、结构化 payload 文案、游客登录引导、下拉刷新、目标失效提示及主题/楼层/用户精确导航。
- 接入通知单条已读、全部已读、删除与独立未读数闭环；底栏角标每 30 秒和回前台刷新，写操作同步角标，乐观已读失败回滚并重新校准服务端未读事实。
- “我的”新增本人收藏管理页：接入游标分页、主题摘要导航与按收藏记录 ID 原地取消；删除分页尾项时回退到仍有效游标，失败保留条目和请求 ID。
- 主题详情接入官方更新与玩家发言两类订阅：普通登录用户可按服务端记录 ID 可逆管理，玩家面板只展示已标记普通玩家，楼主/协作者沿用自动通知并隐藏控件。
- 主题详情接入点赞/取消点赞与收藏/取消收藏：游客点赞先登录并保留帖子目标，登录用户使用服务端权威计数和收藏记录 ID，失败保留原状态与请求 ID。
- 新增关注、粉丝与黑名单移动端列表：公开用户统计进入指定用户列表，“我的”进入本人列表；黑名单支持失败保留、请求 ID 与原地取消拉黑。
- 公开用户主页新增关注/取消关注与拉黑/解除拉黑闭环：认证身份确认后显示，关系写入串行化，拉黑二次确认，失败保留原状态与请求 ID。
- “我的”分支接入本人真实资料与编辑闭环：展示邮箱验证、成长进度和统计，支持独立改名、简介与三项公开范围更新，并保留失败重试和安全退出。
- 公开用户主页新增移动端内容分区：创建主题始终可见，参与主题、最近回复与收藏严格随公开隐私字段显示，四类数据独立惰性加载并支持游标分页、局部错误与目标导航。
- 实现全站搜索移动端闭环：主题、用户、正文三个惰性 Tab，正文游标分页、竞态保护、安全 Markdown 摘要及完整空错重试状态。
- 新增只读公开用户主页，展示公开资料、温油统计与关系状态；搜索结果和 Markdown 用户站内链接统一进入稳定用户路径。
- 新增 `/threads/:threadId?post=:postId` 正文结果定位，自动解析所属子贴并为主楼层或楼中楼补齐、强调目标上下文。
- 锁定并消费 `wenyousite-foundation` v1.1.0 Flutter 包，接入中央语义 Token、移动 profile 与三角色自托管字体。
- 实现 0.3 公开阅读切片：首页主题卡片进入稳定主题详情，详情展示元数据、子贴 Markdown v2 正文、升序楼层分页与服务端内嵌回复。
- 新增安全 Markdown 阅读组件，支持外部链接/图片协议约束和服务端骰子结果展示，并为未知结果提供可复制的降级文本。
- 实现首页推荐主题流首个闭环：公网列表、服务端动态分类、状态/排序筛选、下拉刷新、游标分页及紧凑主题卡片。
- 建立 Flutter Android 工程、固定 OpenAPI 生成链、契约快照、中文模块文档体系与文档门禁。
- 建立初始主题适配、应用环境、网络/会话、本地持久化与 Markdown v2 基线。
- 确定 Flutter Quill 仅作内存编辑模型，并建立自研 Markdown v2 ↔ Delta Codec 边界与测试规范。
- 同步 Markdown v2 扩展节点往返语料和 mobile push v1 Schema/黄金样例，为后续编辑器与推送切片固定跨端边界。
- 实现账号/密码登录、mobile 终端声明、双 Token 安全落库，以及创建主题入口的登录回跳。
- 完成会话刷新与退出闭环：并发刷新锁、单次请求重放、失效原因导航、服务端会话撤销及明确确认的本机退出后备。
- 实现邮箱验证码两步注册、重发冷却、专用错误状态、mobile 双 Token 自动登录及原目标回跳。
- 建立语义主题适配层，以及面板、区块标题、状态提示、异步主按钮、空状态等共享组件。

### Changed

- 开发版本进入 `0.3.0-dev.26+32`；`threadsCreateInviteLink`、`threadsPreviewInviteLink` 与 `threadsJoinByInviteLink` 完整接入，移动端不持久化邀请凭据，外部分享继续由 Web 链接承接。
- 开发版本进入 `0.3.0-dev.25+31`；`threadMembersUpdateMember` 与 `threadMembersExitMember` 完整接入，已废弃的手动加入端点不在移动端暴露，成员身份继续由真实业务行为建立。
- 开发版本进入 `0.3.0-dev.24+30`；`threadsUpdate` 与 `threadsRemove` 完整接入，移动端严格区分协作者和楼主字段权限，更新只发送变化字段与当前版本，删除必须得到服务端明确确认。
- 开发版本进入 `0.3.0-dev.23+29`；`usersMentionCandidates` 完整接入，Foundation v1.1.0 固定工具栏保持不变，移动端通过正文内 `@` 触发候选，新主题未形成服务端草稿时明确要求先保存以取得真实 `threadId`。
- 开发版本进入 `0.3.0-dev.22+28`；`usersSetAvatar` 与 `usersRemoveAvatar` 完整接入，头像只消费服务端明确返回的安全 HTTP(S) 地址，旧头像缓存主动失效且重启后重新读取本人事实。
- 开发版本进入 `0.3.0-dev.21+27`；`usersDeleteMe` 完整接入，后端匿名化账号并撤销全部 refresh 会话后，移动端不会以失败的本地清理结果重放远端注销。
- 开发版本进入 `0.3.0-dev.20+26`；`authVerifyEmail` 与 `authResendVerification` 完整接入，`40107` 保留只读会话和编辑草稿，验证成功不换 Token，只在重读本人资料确认后恢复发布资格。
- 开发版本进入 `0.3.0-dev.19+25`；`authForgotPassword` 与 `authResetPassword` 完整接入，失效会话也可进入公开找回流程，成功后不建立伪会话而是要求使用新密码登录。
- 开发版本进入 `0.3.0-dev.18+24`；`postsFindReplies`、`postsCreate`、`postsUpdate`、`postsUpsertBody`、`postsRemove` 与既有楼层读取全部闭环，`40302/40303`、`40007`、`40002` 和不明确创建结果有稳定恢复状态。
- 开发版本进入 `0.3.0-dev.17+23`；`draftsFindAll`、`draftsSlotUsage`、`draftsFindById`、`draftsCreate`、`draftsUpdate`、`draftsRemove` 全部接入，`40002` 冲突保留当前正文并要求基于最新版二次确认。
- 开发版本进入 `0.3.0-dev.16+22`；主题创建、服务端草稿、聚合发布、媒体上传和本地快照形成首个可真机操作的创作闭环，后端五槽位正文草稿仍作为后续独立切片。
- 开发版本进入 `0.3.0-dev.15+21`；两套后端 Markdown 黄金语料现同时验证规范化、可见性、扩展节点字段和 Delta 往返幂等，损坏 embed 不再允许静默序列化。
- 开发版本进入 `0.3.0-dev.14+20`；`/me/security/password` 与 `/me/security/email` 加入登录守卫，账号安全写入使用独立 autoDispose 状态并保留失败请求 ID。
- 开发版本进入 `0.3.0-dev.13+19`；`/me/security/sessions` 加入登录守卫与安全回跳，终端读取、失败恢复和撤销写入使用独立 autoDispose 状态。
- 开发版本进入 `0.3.0-dev.12+18`；通知占位页替换为 API 驱动消息中心，未知类型与未知目标保持可读且不猜测导航。
- 开发版本进入 `0.3.0-dev.11+17`；`/me/bookmarks` 加入登录守卫与安全回跳，社交模块的主题收藏读取/创建/删除端点全部闭环。
- 开发版本进入 `0.3.0-dev.10+16`；订阅列表与帖内成员候选并发读取，两类写入共享串行状态、失败恢复与请求 ID。
- 开发版本进入 `0.3.0-dev.9+15`；主题详情从只读统计升级为可恢复互动，喜欢计数与操作状态共享同一会话内事实。
- 开发版本进入 `0.3.0-dev.8+14`；五个关系列表读取端点完成映射，私有列表路由加入登录回跳与竞态保护。
- 开发版本进入 `0.3.0-dev.7+13`；社交模块进入 `in_progress`，公开用户关系操作使用独立会话内状态并同步粉丝计数。
- 开发版本进入 `0.3.0-dev.6+12`；“我的”从会话占位页升级为 Foundation 风格的本人资料总览与服务端权威编辑表单。
- 开发版本进入 `0.3.0-dev.5+11`；公开用户页从资料摘要扩展为真实内容浏览，移动端用横向分段切换替代 Web 端纵向堆叠长列表。
- 开发版本进入 `0.3.0-dev.4+10`；搜索与用户模块进入 `in_progress`，动态搜索、用户内容 Tab 和关系写操作按对应模块闭环后续接入。
- 开发版本进入 `0.3.0-dev.3+9`；全局主题迁移到 Foundation 纯白画布、柔粉主色、深紫灰正文、中央状态色和可见焦点，业务流程与 API 状态机不变。
- 删除移动端平行审美指导，模块文档直接引用锁定版本的 Foundation Flutter profile。
- 开发版本进入 `0.3.0-dev.2+8`，主题与楼层模块进入 `in_progress`；本轮保持只读，独立楼中楼页、精确定位、回复及其他写操作后置。
- 首页刷新入口收敛为下拉刷新和错误态重试，主题卡片承担进入公开详情的明确导航职责。
- 详情阅读正文规范调整为约 17sp、1.75–1.85 行高，内嵌回复使用 16sp、约 1.75 行高，并补充主题详情与两层回复的面板规范。
- 开发版本进入 `0.3.0-dev.1+7`，首页从应用壳占位页切换为真实公开内容流。
- 同步后端契约 `4.4.0-dev.20260809.1`：主题分类改为服务端动态配置，主题列表增加封面与温油数据，并接入动态、钱包、成长体系及分平台构建兼容类型。
- 启动兼容门禁切换到后端 `4.x` 主版本，并继续拒绝未审查的未来主版本。
- 第一阶段开发改为相关本地检查与真机冒烟优先；GitHub Actions 暂停自动触发，仅保留阶段验收时手动执行。
- 修复生成 API 客户端与 Dio 基地址重复拼接 `/api/v1`，导致真机启动请求 404 的问题。
- OpenAPI Dart 客户端使用稳定的 `built_value` 序列化，未知枚举安全降级。
- 刷新和退出统一通过生成客户端发送稳定请求 ID；重放再次过期或收到 `40103`～`40106` 时清除本地会话并进入登录页。
- `ApiFailure` 读取 `Retry-After` 并为验证码过期/错误、邮箱已注册和用户名占用提供稳定移动端提示。
- 应用壳、启动状态、登录和注册统一复用共享面板、响应式页边距和至少 48dp 触控区域。

### Contract

- 后端移动接入指南恢复为 `0fb9d35` 的只读同步快照；私有构建与发布运维继续由移动端本地 `mobile-release-operations.md` 维护，不再从后端已移除的路径复制。
- 视觉与跨端体验契约锁定 Foundation `1.1.0`（`4974b09a29d5d1c9632f4b2683c8d36c9e3c69bd`）。
- 后端契约 `4.4.0-dev.20260809.1`，后端提交 `0fb9d351e4344b0bdb347e5530278f02fd0a7418`，Markdown v2；已重新生成 Dart 客户端并清理不再存在的旧管理端模型。
