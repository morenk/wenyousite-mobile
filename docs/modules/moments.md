# 动态

状态：`in_progress`

## 1. 模块目标与非目标

实现独立于主题帖的短内容动态：发现/关注/用户/收藏双列瀑布信息流、详情、纯文本发布编辑、最多九图、本机自动草稿、删除、点赞、收藏、两层评论、评论图片、收藏表情、站内传送门与社区举报。动态正文不解析通用 Markdown，只按后端 v1 黄金契约激活合法站内坐标；不做视频、话题或离线自动发布。

## 2. 用户角色与使用场景

游客可浏览发现流、用户动态、详情与评论；登录用户额外查看关注流和本人动态收藏，发布、编辑或删除自己的动态，点赞/收藏，并发送纯文本、单图或单个收藏表情评论。服务端返回的 `canEdit`、`canDelete` 和评论 `canDelete` 是最终权限事实。

## 3. 页面、入口和导航关系

动态是四分支应用壳的 `/moments` 主分支，分为“发现”和“关注”；顶栏只提供全站搜索入口，不重复展示动态收藏入口。在任一主导航分支点击底栏中央发布动作都会先打开轻量选择器，选择“动态”后进入受保护的 `/compose/moment`。详情使用公开 `/moments/:momentId`，编辑使用受保护的 `/moments/:momentId/edit`，动态收藏使用受保护的 `/moments/bookmarks` 并从“我的”收藏入口进入，公开用户主页通过 `/users/:userId/moments` 读取该用户动态。全站搜索的动态 Tab 以稳定 momentId 进入同一详情；通知 moment 目标也只用服务端 `momentId` 进入详情。

## 4. 用户操作流程

发现流按热度读取，关注流按时间读取；顶部“发现 / 关注”使用共享线性内容页签和底部选中线，不再使用移动端独有的分段按钮，只响应明确点按而不提供横滑换页。发现、关注、收藏和用户信息流以 12dp 间距双列瀑布呈现，卡片只展示封面、两行标题、作者和点赞，正文、评论、收藏与加油进入详情处理。图片封面按服务端宽高预留比例，过长竖图最低收束到 3:4，多图显示数量；列表优先使用 feed，再回退 thumbnail、medium 和主图，GIF 因而在滚动列表默认消费静态派生图。文字封面固定 3:4。独立列表下拉刷新回到首屏，接近底部或按钮加载下一游标；分页追加、反复滚动到底或回顶时保持连续双列和完整滚动范围，加载与结束状态位于两列之后。嵌入“我的”动态页签时，列表加入本人主页的统一滚动链，不安装自己的下拉刷新层，由资料页顶部手势刷新当前用户动态。发布页在同一页面依次展示图片区、2～40 字必填标题和最多 1000 字选填正文，底部安全区只保留全宽“发布”或“保存”主操作。图片区始终显示添加入口与 `n/9`，一次可选当前剩余数量；选中后不打开裁剪页，立即按原顺序显示本地缩略图，横向列表以约三张为当前可视窗口并按需构建，其余滑动查看。后台最多推进两张 `MOMENT` 图片，完成后保持原选择顺序；编辑动态时新增图片遵循同一流程。已完成缩略图优先使用 thumbnail，再回退 feed、medium 和主图。上传进度与失败直接显示在图片及图片区，失败可对同一输入“重新上传”或“取消上传”；取消只清除未完成批次并保留已完成图片，上传中或仍有失败批次时禁止提交。标题、正文、封面与图片顺序 500ms 防抖写入本机草稿；重新进入自动恢复并以 4 秒自动消失的 Snackbar 提供“重新开始”或“恢复原内容”，离页时底部面板只提供“保存草稿并退出 / 不保存”，关闭面板或系统返回表示继续编辑。未完成图片不进入草稿，用户作出退出选择前不取消上传。无图时由服务端返回确定性文字封面主题。编辑提交当前 `version`，删除需二次确认。

详情先以完整标题建立阅读层级，再展示作者、图片轮播、纯文本正文和累计加油；标题/正文保留内边距，轮播只保留页面级外边距，360dp 下宽 336dp。轮播与评论图片优先 medium，再回退 thumbnail 和主图；每个候选真实加载失败后自动尝试下一个，显式全屏预览优先动画/静态主图并可回退派生图。正文与评论只把带名称的合法站内坐标，以及裸 `wenyou.site` / `www.wenyou.site` 主题或 16 位私密邀请坐标渲染为可点传送门，普通 Markdown、外部地址、非法邀请和歧义查询保持完整字面文本；主题、子贴、楼层、讨论、回复与邀请坐标均交给应用路由重新校验权限。多图在固定舞台内一次只显示当前一张，横滑切换不同尺寸时不推动正文。详情首次收藏不传 folderId 参数，由服务端写入默认收藏夹；成功提示保留 4 秒“修改收藏夹”入口并自动消失，用户未操作时不发额外请求，选择自建收藏夹后按稳定动态 ID 移动。非本人动态在顶栏进入 wallet 加油和举报。评论主列表默认最新，控制行左侧显示评论总数、右侧只保留一个设置入口；底部设置面板把顺序和作者选择留在本地草稿，用户点“应用”后才合并为一次首屏重载，非默认草稿才显示“恢复默认”。作者候选失败只影响作者选择，可独立重试，不遮断详情、评论和顺序设置。根评论按主题楼层的连续留白布局，点击非交互区域直接回复，不再常驻“回复”按钮；根评论与楼中楼回复的举报、删除集中到长按操作菜单，楼中楼回复使用与主题帖内嵌回复一致的软面板卡片。详情右下角以不占正文布局高度的紧凑悬浮入口发表评论，列表末尾留出安全空间；顶栏和评论入口始终固定显示，正文与分页评论直接渲染到最终位置，不播放滚动、缩放或入场动画。打开评论后页面入口立即隐藏，普通评论和定向回复只展示唯一的私聊式纯文本 dock：文字独占第一行并横向铺满，图片、表情、实时字数与发送位于下方操作行，键盘避让空间不绘制成可编辑白区。输入器不提供关闭按钮；点击外部暗区或使用系统返回会立即收起键盘与评论编辑器，未发送文字和已完成附件保存在当前详情页输入草稿，再次打开自动恢复且不上传云端；评论最多 500 字，可额外带一张 `MOMENT_COMMENT` 用途图片或一个收藏表情，相册选中图片后直接上传而不打开裁剪页。详情按来源返回，并只使用全局 180ms 水平位移；无来源栈时顶栏和系统返回瞬时进入动态列表。

评论正文文字长按进入系统选字，作者、时间、空白、图片、表情和传送门等非文字区域长按才打开帖子楼层同款中央遮罩窗口；窗口显示评论或回复操作标题，以对称栅格按权限展示复制、删除和举报，暗区阻断背景点击，点按暗区、显式关闭入口或系统返回均可退出。点击评论仍直接回复，菜单不再重复提供回复；评论筛选与输入等完整任务仍使用原有 Sheet。

## 5. API operationId 与生成类型

- 信息流与详情：`momentsList`、`momentsBookmarks`、`userMomentsList`、`momentsDetail`。
- 搜索读模型由 search 模块调用 `searchSearchMoments`，映射为同一 `MomentCard`。
- 内容写入：`momentsCreate`、`momentsUpdate`、`momentsRemove`。
- 互动：`momentsLike`、`momentsUnlike`、`momentsBookmark`、`momentsUnbookmark`、`momentsMoveBookmark`；`momentsBookmarks` 已兼容可选 folderId 参数，当前列表仍读取全部收藏。
- 评论：`momentsCommentAuthors`、`momentsCommentsList`、`momentsReplies`、`momentsCreateComment`、`momentsRemoveComment`。
- 定位上下文：契约已提供第 17 个相关端点 `momentsCommentContext`；当前登记为待接入切片，尚未从通知路由调用。其余 16 个 Moments operationId 已接入移动端主闭环。
- 社区举报：reports 模块调用 `reportsCreate`，分别提交 `MOMENT` 或 `MOMENT_COMMENT` 目标。
- 主要生成类型：`MomentCardResponseDto`、`MomentDetailResponseDto`、`MomentMediaResponseDto`、`MomentRootCommentResponseDto`、`MomentCommentResponseDto`、`MomentStickerResponseDto`、`CreateMomentDto`、`UpdateMomentDto` 与 `CreateMomentCommentDto`。

## 6. 状态模型和数据流

`MomentFeedController` 以 main/bookmarks/user target 隔离列表、游标、刷新、分页和单条互动写入；服务端计数与 active 状态覆盖本机卡片。`MomentMedia` 保存主图、显式派生 URL、contentType、animated 和尺寸，并为信息流与正文分别给出有序候选；仓储和搜索映射器对所有非空 URL fail-closed。`MomentFeedList` 通过滚动通知触发接近底部分页，因此独立页面可使用自身滚动位置，嵌入本人主页时也可继承父级滚动协调器；卡片与全宽尾部状态由同一懒加载瀑布流维护几何，稳定 Key 在刷新重排和分页追加时恢复既有卡片位置。`MomentDetailController` 同时启动详情/主评论和作者候选读取，但把候选加载、失败与重试保存为独立状态；候选失败不改变详情阶段，应用顺序与作者时只重载一次主评论。`MomentComposerController` 负责创建幂等键、编辑版本、冲突重试和删除确认结果；动态仓储与本机草稿存储端口位于 `moments/application`。用户在 `40002` 后选择“保留我的内容”时，控制器先读取最新详情和权限，再以最新 `version` 重交当前表单；选择“使用最新内容”时清除本机草稿并重载详情。动态发布页为未完成图片保留本地输入、独立任务状态和完成结果，最多两张在途；全部完成后一次按原顺序并入已上传列表。评论仍按页面实例消费单图上传任务。

信息流与详情的点赞/收藏状态记录精确在途动作，只锁定对应控件并保留旧选中投影，直到服务端回包覆盖。可写消费者统一使用 Foundation v6.4.0 的点赞/收藏语义色、透明选中容器和官方实心图标变体；搜索等只读消费者始终显示中性线性图标，不暴露按钮或 toggled 语义。

## 7. 鉴权、权限和隐私规则

发现、用户列表、详情与评论读操作允许游客；可选认证请求仍通过普通拦截器发送，以便登录用户取得 viewerLiked/viewerBookmarked 和权限投影。关注、收藏列表及所有写操作要求登录，游客从当前安全路径进入登录回跳。动态编辑/删除只消费服务端 capability；举报入口用同一 `canEdit` 排除本人动态，评论则用当前 JWT `sub` 做保守本人隐藏，最终目标可见性和本人关系都由服务端复核。客户端不猜测隐藏动态、评论或被拉黑关系。预签名 URL、图片字节、评论正文和幂等键不落日志或持久化。

## 8. 本地存储、缓存及失效规则

动态列表、评论、筛选、游标、上传进度和创建幂等键只保存在当前 Riverpod/页面生命周期；评论输入草稿也只保留在当前详情页内存，不写 SharedPreferences 或云端。发布/编辑表单例外：页面通过 `MomentDraftStore` 端口按新建或稳定 momentId 分区，data 适配器将标题、正文、已完成媒体的安全主 URL、可选派生 URL、contentType、animated、尺寸、封面 ID、图片顺序和更新时间写入 SharedPreferences；读取旧草稿时新增字段缺失按空派生图和非动画恢复。不保存源图片字节、未完成批次、预签名 URL 或 Token。进入页面自动恢复草稿但保留进入时基线供用户一键撤销；成功发布、保存、删除、选择最新内容或明确不保存后清除对应草稿。创建/评论请求在结果不明确时保留同一 `clientRequestId`；服务端确认成功后才轮换。图片使用共享安全 HTTP(S) 缓存；发布、编辑、删除和通知返回后失效相关信息流/详情。

## 9. 加载、空数据、错误、重试和冲突状态

信息流空态只保留当前事实标题，不教学下拉、关注或发布。

发现、关注、收藏和用户信息流分别覆盖双列首屏骨架、空、首屏失败、刷新失败和分页失败；接近底部自动加载并保留显式“加载更多”后备入口，`40007` 丢弃失效 cursor 并回到首屏。详情首屏使用单列详情结构 Skeleton，区分不存在与网络失败，已加载内容刷新失败时继续保留可读事实；评论作者候选失败在设置面板独立恢复。`40002` 显示“这条动态刚刚更新了”，并明确提供“保留我的内容 / 使用最新内容”，不静默覆盖本机输入；`40415` 收敛为动态或评论已不存在；不明确创建/评论失败保留原输入与同一幂等键。上传失败在当前本地缩略图与图片区复用同一图片重试或取消剩余批次；已经完成的同批图片保留，处理中与未解决失败批次均禁止提交，只有用户确认退出后才取消任务并忽略迟到结果。

## 10. 跨模块约束

动态作者头像、发布时间与互动计数复用 Foundation 共享身份和格式化组件；短时间同时提供完整时间语义，紧凑计数的操作语义保留服务端原值。

media/application 提供相册与上传端口、任务状态、取消、失败和同文件重试；moments presentation 不直接导入 media/data 或 Dio，只消费完成结果并维护最多九图、封面顺序及评论图片/表情互斥。stickers 提供用户私有收藏选择器；notifications 只传稳定 momentId；users 暴露公开用户动态入口，并在本人主页负责唯一的页面级刷新手势与资料头滚动协调，moments 只提供当前 user target 的列表和刷新入口；search 复用完整宽度动态卡片但不持有动态详情或写入状态；wallet 维护动态加油幂等写入；reports 管理动态与评论举报。动态正文和评论始终是纯文本，不进入 Markdown/Quill Codec；`core/navigation/internal_reference.dart` 只消费独立的 `wenyousite-internal-reference` v1 契约，不能扩大成通用 Markdown 解析器。合法站内坐标由共享阅读组件渲染成 Foundation v6.4.0 传送门胶囊，非法坐标与普通 Markdown 保持字面文本。视觉只消费 Foundation v6.4.0 Token、元素契约、语义图标与图片角色；创作缩略图固定使用 `galleryThumbnail` 的 cover、允许裁剪和圆角语义。发现、关注、收藏和用户信息流显式消费 `WenyouCollectionContract.mobileDomainLayoutExceptions['moments-feed']` 后使用双列瀑布流；详情继续保持单列和不超过 600dp，独立操作命中区不小于 48dp。

## 11. 测试场景与验收条件

- [x] 已接入的 16 个 operationId 的分页、DTO、幂等、版本与删除确认有仓储测试。
- [x] 未知枚举、不安全 URL、重复 ID、异常计数/层级和图片/表情互斥采用 fail-closed。
- [x] 信息流 `40007` 重置、服务端互动计数、评论幂等重试、楼中楼分页/筛选和删除校准有控制器测试。
- [x] 评论总数与单一设置入口同行；顺序和作者先保留本地草稿，显式应用只重新加载一次，恢复默认仅在非默认草稿出现；作者候选失败不阻塞详情或评论并可独立重试。
- [x] 发现/关注游客边界、纯文字发布、详情/评论和 360/400/600dp 布局有 Widget 测试。
- [x] 动态列表顶栏只保留搜索，不重复展示动态收藏入口；收藏列表仍可从“我的”进入。
- [x] 独立动态列表保留下拉刷新；嵌入“我的”的用户动态不重复安装刷新层，滚回列表顶部后继续展开资料头，并保持接近底部分页。
- [x] 360dp 双列瀑布续排、首屏骨架、列表信息层级、TalkBack 标签、48dp 点赞命中区和 Foundation 视觉基线有回归测试。
- [x] 混合长短标题与横竖封面反复滚动到底、回顶及分页追加后，双列位置、全宽尾部状态和可滚动范围保持稳定。
- [x] 点赞/收藏的 Foundation v6.4.0 语义色、透明容器、官方实心资产、提交锁、最小命中区与只读中性语义有 Widget 回归。
- [x] 动态首次收藏进入默认收藏夹，4 秒提示可打开动态数量口径的收藏夹选择器并按动态 ID 移动，随后自动消失；未操作不发送移动请求。
- [x] 纯文本内合法传送门的胶囊、门图标、换行不截断、48dp 命中区和内部导航有 Widget 回归。
- [x] 详情多图以固定比例单图横滑轮播，切换不改变舞台高度，当前位置与对应原图入口有 Widget 回归测试。
- [x] 360dp 详情轮播宽 336dp，标题/正文独立留白；评论、通知、会话和楼层采用扁平连续内容样式。
- [x] 详情评论入口在 360/400/600dp 使用 48dp 内容宽度固定悬浮按钮，不压缩正文视口；滚动、加载和身份变化均不播放顶栏、正文或入口动画。普通评论、定向回复、软键盘避让和本页输入草稿复用唯一的私聊式单体 dock，点击暗区立即收起且再次打开恢复，IME 最终 inset 在首帧直接生效而不跟随键盘动画升降，附件、字数和发送保持在同一紧凑表面，避让区域保持透明且不接入 Markdown/Quill。
- [x] 动态发布/编辑使用“图片—标题—正文—唯一底部主操作”单页流程；多选后不经裁剪立即展示本地缩略图，以约三张可视窗口按需构建并最多推进两张后台任务，全部完成后仍按原顺序合并；支持点按封面、长按排序、移除回退，并自动保存文字、封面和图片顺序，进入时自动恢复且在 4 秒提示内可撤销，提示关闭后草稿内容仍保留；离页只显示保存或不保存。
- [x] 动态发布与评论共用 media/application 上传任务；发布批次失败可复用原图片重试或取消剩余批次，已完成图片保留，上传中与未解决失败批次禁止提交；并发上限和完成顺序有回归测试。
- [x] 发布与评论分别传入 `MOMENT` / `MOMENT_COMMENT`；信息流、详情、评论和创作缩略图按用途消费派生图并在真实加载失败时逐级回退，动画主图只在显式全屏优先使用。
- [x] 本机草稿往返保存派生 URL、contentType 和 animated，并兼容缺少这些字段的旧草稿。
- [x] 动态编辑冲突覆盖两种显式选择，保留本机内容时以最新版本重试，使用最新内容时清除草稿并重载；360dp 纯文字/多图视觉基线、键盘态、TalkBack 图片动作和 360/400/600dp 布局均有回归。
- [x] 动态通知、用户动态、收藏、创建与编辑路由使用稳定 ID，并有导航/回归测试。
- [x] 动态详情保留真实来源栈；直接进入时顶栏和 Android 系统返回均回动态列表，不退出应用。
- [x] 全站动态搜索的短词、分页、失效 cursor、安全 DTO 映射和详情路由有自动测试。
- [x] 非本人动态可进入加油，累计金额保持十进制整数字符串且成功后重读详情。
- [x] 非本人动态、根评论与回复可举报；评论举报和服务端允许的删除操作只在长按菜单中出现，本人及已删除内容隐藏对应入口，游客保留完整登录回跳。
- [x] 动态根评论与楼中楼回复支持整层直接点击回复，回复按钮和菜单回复项均不重复出现；正文长按选字，元信息、空白与非文字节点长按打开带标题的对称操作窗，楼中楼回复复用主题帖内嵌回复软面板组件。
- [x] 动态评论和定向回复的正文独占输入 dock 首行，320dp、2.0 倍字体下不被图片、表情、字数或发送挤窄；点击外部暗区或系统返回收起并保留当前详情页草稿，360dp 键盘态由 Golden 固定。
- [x] v1 黄金 fixture 的 14 个坐标与 5 个渲染用例全部消费；正文/评论传送门进入应用路由，非法邀请、普通 Markdown 和外链保持字面文本。
- [ ] 第 17 个相关端点 `momentsCommentContext` 从通知导航接入，完整消费 `momentCommentNavigation` 的主评论、楼中楼、墓碑、404 和临时失败旅程，覆盖上下文补取、目标滚动与 1dp 淡粉边框短时提示。
- [ ] 使用公网专用账号完成发现/关注、九图发布编辑、原图、点赞收藏、图片/表情评论和删除真机联调。

## 12. 已知限制和后续功能

动态发布支持按剩余容量一次多选、不经裁剪并以有界流水线处理，评论附件仍一次一张且同样不经裁剪；本机草稿不跨设备同步，也不后台自动发布。未完成本地缩略图只存在于当前发布页，页面或进程释放后不能恢复。全屏图库具备规范核心手势，但暂不显示下载/分享入口。动态加油已由 wallet 接入；当前不在信息流卡片直接弹出写入，以详情作为稳定确认上下文。动态快捷收藏已支持移动到其他收藏夹，但本人动态收藏页尚未按收藏夹筛选，公开用户页尚未接入公开动态收藏分页。传送门 v1 只做语法识别，不预取目标标题或权限，最终可见性由目标页面服务端请求决定；邀请 token 除正文/评论本身外不写日志、诊断或独立缓存。动态评论通知目前只进入详情；`momentsCommentContext`、`momentCommentNavigation`、目标滚动和短时淡粉边框需作为同一切片接入，不能只拼接一个可能尚未加载的评论 ID。

## 13. 最近审查的契约版本和后端提交

契约 `5.10.0-dev.20260823.1`；Markdown v3（动态正文不消费）；后端 `6446a3ffd3f8c88613ea6f54128a44ac96d372d5`；Foundation `v6.4.0`（`0297a99`）。

## 14. 相关代码与架构文档

端口与状态：`lib/features/moments/application/`；API 与本机草稿适配器：`lib/features/moments/data/`；页面：`lib/features/moments/presentation/`。参见[导航](../architecture/navigation.md)、[API 生成与覆盖审计](../architecture/api-generation.md)、[媒体](media.md)、[收藏表情](stickers.md)、[温油钱包](wallet.md)、[社区举报](reports.md)、[通知](notifications.md)与[用户](users.md)。
